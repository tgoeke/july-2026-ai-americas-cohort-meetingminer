"""MeetingMiner server package.

All server code lives under this namespace (story 1.10) so the installed
distribution never claims generic top-level module names like ``config``,
``api``, or ``pipeline`` that a future dependency could shadow.
"""
