# .working — audit trail of this run

- `color-system-a-signal.html` (picked), `color-system-b-spectrum.html`, `color-system-c-ember.html` — the three color systems, every text-on-surface pair measured by script.
- `direction-mono-dense.html`, `direction-relaxed-cards.html` — the two design directions (relaxed cards picked for Moments only).
- `screens/*.jpeg` — headless-Chromium screenshots taken to verify the studies and the key-screen mocks open as local files.
- The seven key-screen mocks were rendered here as `key-*.html` and promoted to `../mockups/` under their spine-linked names; the copies here were retired at Finalize so `mockups/` is the only version.
