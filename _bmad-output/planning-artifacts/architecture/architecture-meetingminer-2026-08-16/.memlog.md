---
scope: MeetingMiner capstone whole-system architecture spine from SPEC-meetingminer (CAP-1..8) + companions
purpose: build-substrate; plus instructor-facing solution design doc at finalize
altitude: feature
updated: 2026-08-31T08:56
---

- (direction) Coaching path chosen; deliverables = spine + prose solution design doc for InfoQ instructors; greenfield repo (no code yet); inherits SPEC constraints silently (no-citation-no-answer, deterministic core, adapters, one-way publish, precompute-before-view, solo dev ~1wk demo)
- (event) Run resumed 2026-08-17; spec advanced since run start: CAP-9 multi-store retrieval (full-text doc index + GraphRAG, both derived projections), database-of-record constraint, publish-gate indexing, OCR=Apple Vision primary/Tesseract fallback, judge bake-off, ocr_anchor script rule — all inherited silently
- (decision) Frontend is React (user-fixed; rejects Python UI toolkits). Backend pipeline is Python. Binds: all UI work; Prevents: split UI stacks
- (decision) Everything runs on the user's Mac laptop (M4 Max, 128GB); Docker preferred for runtime where feasible; logical component separation in a microservices-style structure (backend API, video-processing jobs, ...) even if deployed locally
- (constraint) All architecture diagrams must be proper mermaid (graphviz dot acceptable substitute) — never ASCII art. Applies to spine, solution design doc, and in-conversation sketches
- (note) Existing JS puller script (Teams recap pull, some SharePoint integration — possibly irrelevant) available from user; msft-sandbox-setup-guide.md documents the Teams sandbox infra
- (direction) User wants a facilitated brainstorm/selection for RAG, GraphRAG, other AI components, and eval tooling — options researched on the web, presented with leanings
- (note) Sandbox guide binds the Teams integration surface: app-only Graph (client-credentials), channel meetings required for SharePoint-side storage + transcript API, application access policy + Sites.Selected grants, upload sessions >4MB. Capstone uses Graph only to SOURCE recordings/transcripts (publishing stays local folder+git per spec non-goal); existing JS puller covers part of this
- (constraint) macOS-native lanes cannot run in Docker: Apple Vision OCR is a macOS framework; MLX/Metal GPU and Ollama acceleration are host-only. Any runtime split must keep OCR/STT/local-LLM adapters on the host
- (decision) Runtime split: stateful infra (DB of record, search engine, graph store if separate) in docker-compose; Python API + pipeline workers + React dev server run on macOS host with Apple Vision/MLX/Ollama access; one make-up style command starts everything. Binds: deployment topology; Prevents: workers landing in containers that cannot reach macOS-native adapters
- (version) Graph research (web-verified 2026-08-17): Kuzu DEAD upstream (Apple acq, repo archived Oct 2025; LadybugDB fork young); MS GraphRAG v3.1.1 maintenance-mode + auto-extraction mismatch; LightRAG wants to own the graph; Neo4j Community 2026.07.1 active w/ GA vector SEARCH in CE + neo4j-graphrag 1.18.0; Postgres 18 + pgvector 0.8.2 active; Apache AGE 1.7.0 rough. Top-2: (1) Postgres18+pgvector as DB-of-record w/ hand-rolled deterministic traversal templates; (2) Neo4j CE + neo4j-graphrag retrievers w/ hand-written Cypher
- (version) STT/adapter research (web-verified 2026-08-17): mlx-whisper 0.4.3 (Metal, word timestamps) + parakeet-mlx 0.5.2 (faster, sharper timestamps, EN) both viable; faster-whisper CPU-only on Mac; diarization skippable (Teams VTT carries speakers; pyannote 4.0.4 documented opt-in behind no-op Diarizer adapter); LiteLLM 1.97.0 de-facto adapter over Anthropic/OpenAI/Ollama/OpenRouter-Kimi (any-llm 1.19 lighter fallback); Ollama 0.32.6 host-run w/ mature OpenAI-compat endpoints; embeddings local-first qwen3-embedding:0.6b (1024d) via Ollama, hosted swaps OpenAI 3-small / voyage-4 (1024d compatible)
- (version) Backend/jobs/eval/frontend research (web-verified 2026-08-17): FastAPI 0.141.1 standard (Litestar hasn't displaced); job orchestration → hand-rolled Postgres job-state runner on host is genuinely right (arq maintenance-only; RQ 2.11 lightest real queue if forced; Prefect/Dagster/Temporal overkill; queue workers in Docker can't touch Apple Vision/MLX anyway); eval → pytest 9.1 skeleton w/ YAML fixtures + run-dir artifacts (Inspect AI 0.3.259 optional viewer, not spine; promptfoo Node-side awkward; Braintrust/LangSmith cloud-metered mismatch); frontend → Vite 8 + React 19 SPA + shadcn/ui CLI v4 Vite template + native HTML5 video for timestamp replay + hey-api/openapi-ts 0.99 typed client
- (version) Full-text search research (web-verified 2026-08-17): Meilisearch 1.53.1 (arm64, MIT core, GA hybrid+auto-embedders, typo tolerance/facets/highlighting, first-party Python SDK 0.43) top pick as sidecar projection; ParadeDB pg_search 0.25.3 (BM25-in-Postgres, AGPL, pre-1.0 churn, arm64 image w/ pgvector) top one-fewer-part pick; plain PG FTS honest weakness = no IDF/BM25 ranking; OpenSearch/Elastic ops-heavy for laptop demo; SQLite FTS5+sqlite-vec alpha-risk fallback; Typesense 30.2 fine but GPL server + more schema ceremony
- (decision) DB of record = Postgres 18 (+pgvector); domain graph modeled relationally with FKs. Neo4j Community 2026.07 runs as a SECOND container holding a derived, rebuildable graph projection traversed via hand-written Cypher templates (neo4j-graphrag 1.18 retrievers optional). Binds: storage + GraphRAG topology; Prevents: Neo4j becoming primary copy, framework auto-extraction owning the graph
- (decision) Full-text retrieval store = Meilisearch 1.53 sidecar container; reindex = replay documents from Postgres. Binds: CAP-9 doc-index store; Prevents: search index living inside the record DB and blurring the projection story
- (decision) Model adapter layer = LiteLLM >=1.97 wrapped in project-owned thin interface; providers (Anthropic/OpenAI/Ollama/OpenRouter-Kimi) swapped by config string; embeddings default Ollama qwen3-embedding:0.6b @1024d, hosted swaps (OpenAI 3-small, voyage-4) at same dimension. STT lane: mlx-whisper default + parakeet-mlx alt behind one adapter; Diarizer adapter no-op default (pyannote documented opt-in). Binds: every model call path; Prevents: provider SDKs called directly from feature code
- (decision) Eval harness = pytest 9.1 skeleton: YAML ground-truth fixtures, tier-1 deterministic tests, tier-2 marked LLM-judge tests, plugin writes evals/runs/<run-id>/ + verdict lock. Binds: CAP-7/CAP-8 implementation shape; Prevents: bending run-dir/runbook design to a framework's log format
- (decision) Adopted defaults (no-fork, web-current): FastAPI 0.141 API; Vite 8 + React 19 + TS + shadcn/ui SPA; native HTML5 video for timestamp replay; hey-api/openapi-ts typed client; ingestion jobs = host-side Python worker + job/stage rows in Postgres (no broker), stage-checkpoint restart
- (decision) Puller stays the existing JS/Node CLI, kept as a black box behind the source-drop seam; adapted to sandbox tenant + drop format; SharePoint parts stripped/ignored this capstone. Binds: source acquisition component; Prevents: rewrite risk and ingestion knowing about sources
- (decision) Paradigm: deterministic evidence pipeline core + ports-and-adapters at non-deterministic/external boundaries + CQRS-lite (Postgres single write model; Neo4j/Meilisearch read projections). Proposed invariants: (1) one canonical source-drop inbox format for all sources; (2) one-writer-per-store — worker owns evidence tables, API owns approval/publish tables, projections written only via one shared projection module + rebuild CLI; (3) citations = Postgres-minted moment IDs carried verbatim into all stores and answers; (4) binaries on filesystem under one content root, DBs store relative paths; (5) publish gate enforced inside the projection module
- (decision) Egress stance: NO system-wide restriction — any configured provider may receive any content. Note: eval-design.md's narrower judge rule (cloud judges receive derived data only per allowlist) left intact as an eval-scoped rule unless user relaxes it there too. Binds: adapter payload policy; Prevents: building an allowlist enforcement layer
- (decision) Default model bindings: extraction + chat synthesis = frontier cloud model (claude-sonnet-5 via LiteLLM) primary; Ollama local models configured fallback + live config-swap demo beat; judge = bake-off winner per eval-design. Binds: shipped config.yaml defaults; Prevents: demo-day model ambiguity
- (decision) Accepted proposals (presented, unopposed): API-inserts-job-row/worker-advances (no in-process pipeline work; poll or LISTEN/NOTIFY); idempotent checkpointed stages; deterministic citation validator gates every chat answer (no-citation-no-answer enforced in code); single versioned config.yaml (env for secrets) declares all adapter bindings incl. per-role LLMs, recorded into eval run metadata; REST + SSE (job progress, chat streaming) + OpenAPI-generated TS client; monorepo seed server/ web/ puller/ evals/ infra/ docs/
- (note) Egress rationale (user, 2026-08-17): corpus is scripted sandbox Teams meetings — no data-control concerns in the data set; provider access uses corp corporate API keys, which are within the approved data-control set. Confirms AD-12 no-restriction stance
- (event) Reconcile vs SPEC+scope: 7 findings — (1) no transcribe/merge stage in pipeline seed; (2) merge-dont-erase rule for provided transcripts uncarried; (3) AD-5 contradicts artifact lifecycle column ownership (needs column-level rule); (4) stage order frames→screens→ocr conflicts with OCR-text-compare dedup; (5) Morning Digest COULD dropped; (6) participants path ambiguous (puller metadata vs adapters/graph_source); (7) eval-before-demo-script sequencing silent
- (event) Reconcile fixes applied: AD-1 amended (puller owns Graph participant resolution; local drops -> API-declared participants; no server Graph calls); AD-4 amended (evidence projects at ingest, artifacts only on publish; search/chat scope = evidence + published artifacts; right-rail reads Postgres); AD-5 amended (artifacts table split by column: worker owns extraction content, API owns lifecycle); AD-12 amended (eval-design 'endpoint allowlist' resolves to config.yaml provider endpoints); new AD-13 (provided transcripts immutable; merge never erase; AD-11 overwrite applies to derived rows only); pipeline stage list fixed (ocr before screens, transcribe+align added, OCR-text-similarity screen identity); graph_source/ adapter removed; puller keeps own .env; CAP-7 row notes eval-before-demo-script build order; Morning Digest COULD added to Deferred. Upstream defect noted: eval-design §7 cites a SPEC locality constraint that doesn't exist in SPEC.md
- (event) Reconcile vs eval docs: PASS except allowlist-term flag (fixed in AD-12). Reconcile vs ux+sandbox: PASS except participants ambiguity + published-only chat scope (both fixed); sandbox guide fully compatible with puller black box
- (event) Gate/rubric: approve-with-revisions; 1H+3M+5L. H: source-drop metadata.json contract needs schema location/version/casing. M: unpublish story absent (git one-way); embedder swap must force projection rebuild (1024-dim tension AD-8 vs AD-10); Postgres/content-root recovery silent (fix: re-ingest from immutable drops). L incl. speaker attribution for local-no-transcript-noop-diarizer path. Full review reviews/review-rubric.md
- (note) Puller reality (brownfield read 2026-08-17): pull_transcript/ is a Playwright Stream-page scraper (NOT Graph API) — built around corp-tenant permission walls; per-occurrence output <Title>/<M.D.YY>/ with speaker-attributed .txt (from transcript API json), .vtt, .docx, .mp4 when downloadable, _source.json provenance, pulls.jsonl ledger; also generates Ollama summaries/action-items (ignored at intake). ~25 pulled corp meetings usable as dev corpus. Sandbox tenant makes permission workarounds moot
- (event) Gate complete. Web-verification: PASS 18/18 (CE VECTOR-type caveat noted in Deferred). Adversarial: 3C+3H+4M — all closed: AD-1 pinned drop JSON Schema; AD-14 one intake door (POST /ingests, worker mints Meeting); AD-5 participants column-split w/ dedup key (AAD id else case-folded name); AD-16 evals = API client + read-only asserts; AD-4 projection module owns embeddings via port (store auto-embedders disabled), lifecycle one-way (no unpublish); AD-15 citation wire format ([[moment:uuid]] markers -> structured citations array); AD-11 rerun scope excludes cross-meeting upserts; SSE event names pinned; recovery convention (drops = immutable recovery root); AD-8 embedder swap forces rebuild + eval rerun; AD-13 Unknown-speaker placeholder
- (event) Editorial pass (structure+prose lenses) on solution-design.md: 3 condensations applied (judge-model sentence -> §7 pointer w/ candidates moved into Tier-2 box; GraphRAG parenthetical -> §8 pointer; arq container clause -> §3 pointer), dev-corpus paragraph moved §7->§4, 6 prose fixes (Apple acquisition phrasing, GA + BSL expansions, database-of-record term alignment, §2 parallel structure, 'Built in this capstone'); 4 PRESERVE rulings honored
- (event) spine finalized
- (direction) User (2026-08-17, update intent): Design Paradigm section too abstract — wants a component diagram with high-level design and more detail in the spine. Amend: consolidate into a Component Design section (detailed component diagram, per-component responsibilities/interfaces, query flow); keep paradigm as one-liner; AD IDs unchanged
- (event) Spine updated: Design Paradigm section replaced by Component Design — paradigm kept as one-liner; added detailed component diagram (web/api/worker/pipeline/projections/adapters/domain/puller/evals internals + store edges), per-component Owns/Exposes/Consumes table, cited-Q&A sequence diagram incl. rejection path; dependency-direction diagram retained. AD IDs unchanged
- (direction) User terminology rule: no metaphorical phrasing in design docs ('owns truth', 'source of truth', 'load-bearing') — replaced with precise mechanism statements: model outputs confined to extracted artifacts (human-approved) + citation-validated answer prose, evidence records written by deterministic code only, Postgres sole authoritative store, 'primary query path'. 'Ground truth' retained as the precise ML term. Note: SPEC.md constraint still reads 'AI never owns truth' — flagged to user, not edited (spec is upstream)
- (event) Diagram review top-to-bottom (10 diagrams, both docs). Root cause of jumble: component diagram mixed abstraction levels (internal functions drawn as nodes). Reworked: spine HLD now one-node-per-component in layers (Clients / Services / Server libraries / Stores / Filesystem / Providers) w/ protocol-labeled edges + footnotes for projections/adapters mediation; internals relegated to responsibilities table, Server module structure diagram (renamed from Dependency direction), and new linear pipeline-stages diagram; Q&A sequence moved after module structure; deployment diagram fixed (evals runs via API not worker; filesystem grouped; pytest evals added as host process); solution-design §3 aligned to same layered HLD. Kept unchanged: Q&A sequence, module deps, both ERDs, ingest sequence, publish loop, judging pyramid. Mermaid retained over dot (GitHub renders mermaid inline; dot would need pre-rendered images)
- (direction) User (2026-08-18, update intent): M365 developer sandbox discontinued (spec memlog 2026-08-18). Amend spine: drop msft-sandbox-setup-guide.md source (file deleted), fix Structural Seed egress line, reflect amended CAP-1 (puller-emitted drop directory; participants from transcript speakers + sidecar; no server Graph), carry scope.md corpus split. epics.md AD-1 already reflects the plan
- (decision) AD-1 amended (corp-tenant plan): sources are the corp production Teams tenant via the existing pull_transcript puller — outside MeetingMiner, Playwright Stream scrape logged in as the user, emit-drop step assembles drop dirs (recording + speaker-attributed transcript export + metadata.json sidecar per docs/source-drop.schema.json) into the drops folder on the dev Mac. Participants: sidecar carries whatever the puller supplies (best-effort); pipeline derives from transcript speaker attribution when absent; Graph participant lookup is product-later. Binds: source acquisition; Prevents: MM-to-Teams integration, server-side Graph
- (decision) Structural Seed egress corrected: external egress from MeetingMiner is provider APIs only (Anthropic/OpenAI/OpenRouter); the puller (outside MM) reaches the corp Teams tenant. Deployment diagram: corp tenant node replaces 'Microsoft Graph (sandbox tenant)'; drops folder added to filesystem
- (decision) Corpus split (scope.md Corpus) carried into CAP-7 map row: only scripted corp-tenant mock meetings are eval subjects (ground-truth manifests, 100% capture recall); the ~25 real pull_transcript meetings are ingested demo corpus — searchable, visible in demo, never eval subjects
- (note) AD-12 unrestricted-egress stance survives the corpus change: user accepted client content (vendor/project/Boomi/corp) visible in the demo (spec memlog 2026-08-18); provider access still via corp corporate API keys
- (event) Update reviewer gate (2026-08-18): lint PASS (1 pre-existing false positive: REST path {id}); rubric PASS-with-fixes (5 findings); web-verification sound (sources paths off-by-one, puller credentials = .transcript-profile browser session not .env — both fixed; all puller claims verified against pull_transcript/ on disk); adversarial 2C+4H+3M+2L. Full reviews in reviews/review-{rubric,web-verification,adversarial}-update-2026-08-18.md
- (decision) AD-14 tightened: POST /ingests with a sourceId that already has a non-failed job returns RFC 9457 conflict — re-processing is a rerun of the existing job, never a second Meeting row. Prevents: re-pull + unconditional POST duplicating meetings and splitting citations
- (decision) AD-5 tightened: participant dedup key = AAD object ID else normalized display name (case-fold, strip parenthetical qualifiers, reorder 'Last, First'); human merge writes API-owned alias row (alias_key -> surviving id) which the worker resolves before insert. Prevents: name-format fragmentation on real corp data ('Blake, Cameron' vs 'Cameron Blake'); merges resurrecting on re-ingest
- (decision) AD-13 tightened: multi-form transcript precedence — speaker labels from the speaker-attributed export, cue timing from VTT when present, align reconciles by text alignment, never picks one file wholesale. Prevents: compliant builds diverging between all-Unknown-speakers-with-ms-timing and named-speakers-with-fabricated timing
- (decision) Gate fixes, structural: drops folder is a dedicated write-once directory distinct from the puller's mutable working archive; one-time backfill emit pass converts existing occurrences to schema-valid drops; puller's contracts = drop format (AD-1) + POST /ingests (AD-14), settling who calls intake; recovery convention scoped to evidence — curated state + publish git repo need pg_dump + publish-folder backup; puller auth = .transcript-profile browser session (no credential files); frontmatter sources paths corrected to ../../../specs/; puller vendored-in-monorepo vs runtime-boundary clause added
- (question) Upstream drift found by gate, NOT edited here: (1) epics.md FR1 + Story 1.8 still authorize 'Microsoft Graph where accessible' puller-side, which amended SPEC non-goals arguably rule out — user should reconcile via epics update; (2) no epic story owns the backfill emit pass (Story 1.8 covers URL-triggered pulls only); (3) eval-design 1 should state ground-truth times are offsets from recording start (record-press = t0); (4) AD-12 egress rationale re-confirmed via memlog note only — real client meetings now flow to cloud providers under corp corporate keys per user's demo-corpus acceptance
- (event) spine updated (corp-tenant plan, 2026-08-18): sandbox source removed, egress corrected to provider-APIs-only, AD-1/AD-5/AD-13/AD-14 amended, corpus split carried to CAP-7; solution-design aligned; AD IDs stable
- (event) Update run 2026-08-19 ABORTED before any edit: the augmenting-drop mechanism was already decided and shipped by the 1.12 builder on branch story/1-12 (13 commits, spine amendment 643e48a). Architect's drafted amendment to AD-1/AD-4/AD-5/AD-11/AD-13/AD-14 was reverted uncommitted; main left clean at ecf7aee. Role repointed from decide to review-and-ratify. Builder owns ARCHITECTURE-SPINE.md until the branch merges
- (event) Ratification of story/1-12's AD-1/AD-14 amendment (read-only review, verified against branch): SOUND, and better than the architect's draft on two counts. (1) schemaVersion enum [1,2] with an allOf making augments imply v2 fails closed on a v1-pinned consumer; the architect's optional-at-v1 alternative would let a stale consumer ignore the field and ingest the recording as a second meeting. (2) Intake re-arms the occurrence's existing job in place (new drop_path, AUGMENTATION_STAGES back to queued, 200 with the existing job id) rather than opening a second job — faithful to AD-14's existing 'rerun of its existing job' wording, and it relaxes none of meeting.job_id UNIQUE / meeting.source_id UNIQUE / job_source_id_live_key. WITHDRAWN as unnecessary: the architect's proposed resolve-or-mint, job-to-meeting many-to-one, and the AD-11 rerun-partition rewrite — with one job per meeting, 'rows keyed to that job's meeting' still partitions, so the hazard does not arise
- (question) Post-merge spine gap 1 (builder concurs, nothing states it): moment identity is stable only because transcript_segment.start_ms carries the PROVIDED transcript's cue timing while the STT lane writes stt_start_ms + alignment_delta_ms in separate nullable columns (migration 0005:193-194) — never over start_ms. That is what keeps identity_key 'transcript:<start_ms>' fixed when a recording arrives, and therefore what keeps every pre-existing citation resolving. Today it is an implicit property of 0005's column layout plus a passing test (test_augmentation.py), so a future capture/segmentation retune could break it silently. Belongs as one sentence in AD-13 (which already owns merge-never-erase) or AD-6
- (question) Post-merge spine gap 2 (code/product call, not on the branch): AUGMENTATION_STAGES re-queues 'moments', an EVIDENCE_STAGE, so evidence_complete() returns false during augmentation and api/meetings.py:91 reports viewable=false for an already-complete, already-cited meeting. api/meetings.py is unchanged on story/1-12. Projections are untouched until completion, so search/chat keep working — the flip is the safe-to-open list gate only. On the routine path across ~169 transcript sources this will be visible often
- (question) Post-merge spine gap 3: the Recovery convention says drops are the immutable recovery root and evidence is reconstructed by re-ingesting drops, but re-arm replaces job.drop_path and deliberately keeps job.source_id/corpus, so the DB records neither the first drop's path nor the recovered recording's own asset id (the augmenting drop's own sourceId is discarded). Reconstruction therefore requires replaying a meeting's drops in emit order — the augmenting drop alone is refused, since intake needs the target occurrence to exist. The Recovery row should say so
- (question) Pre-existing spine/code drift found on main, unrelated to 1.12: AD-5 states participant dedup is 'AAD object ID when present, else normalized display name', but docs/source-drop.schema.json calls Graph/AAD an explicit non-goal and pipeline/speakers.py keys on the source-supplied mail address (MAIL_NAMESPACE 'mail:') falling back to normalized display name. The spine contradicts shipped code and the drop contract; ratify AD-5 to mail-else-normalized-name in the next Update
- (question) CORRECTION to post-merge gap 2 (builder's mechanical argument, accepted): viewable=false mid-augmentation is CORRECT gating, not a regression — the earlier entry's 'already-cited meeting reads as unviewable' understated it. align.py:598 does a wholesale DELETE FROM transcript_segment WHERE meeting_id, and moment_segment CASCADEs on both sides (0006_moments.sql:70-77), so between align starting and moments finishing the meeting's moments exist with their ids intact and ZERO transcript coverage — opening it would render moments over no words. Widening the gate would be the regression. The real gap is narrower and is an architecture invariant that constrains Epic 2 rather than waiting on it: an augmenting run must be distinguishable at the API from a never-ingested one, because the meeting keeps its identity, citations and projections throughout — derivable today from job.status plus job_stage with no schema change, so Epic 2 must not key its empty state on viewable alone
- (decision) Neighbouring trade to state beside the identity invariant (builder, story 1.12): augmentation invalidates the meeting's projection state rather than calling unproject_meeting, deliberately — the meeting stays answerable from its transcript in both stores for the length of the re-run instead of blanking out of search and chat. Documented cost: a concurrent rebuild re-inserting the projection state row restores ACTION_NONE and the augmented bundle then never projects; remedy is rebuild --meeting. Binds: AD-4 projection invalidation on augmentation; Prevents: a re-run silently removing a cited meeting from retrieval
- (direction) User (2026-08-19, update intent) forwarding the storage-layout writer's hand-off: two items queued for the architect. (1) AD-3 states one anchor where the code has always had two — drop-relative for arriving material, content-root-relative for produced material — and reading it literally produced the withdrawn copy-the-video story. (2) The Structural Seed deployment paragraph still reads 'one environment only: the dev/demo MacBook ... external egress is provider APIs only', which the LAN Ollama host and the new on-prem GPU host already contradict. Spec side already landed at 949c293 (storage-layout.md companion + two SPEC constraints + stories 2.1a/2.1b); this run records the spine half
- (decision) AD-3 amended in place (two anchors): two configured permanent roots, MM_DROPS_ROOT and MM_CONTENT_ROOT; which root a path is relative to is a property of how the file came to exist (arrived vs produced), not of its type. Arriving material — recording, provided transcripts, metadata.json — stays in its write-once drop and is recorded as <drop-dir>/<filename> relative to the drops root; produced material — frames, screenshots, extracted audio — is recorded as meetings/<meeting_id>/<subdir>/<filename> relative to the content root. Neither root's absolute path is stored in a database or leaves the server; both are resolved at use time. The recording is NOT copied under the content root: AD-1 already makes the drop permanent, and a copy would fix replay while leaving transcript re-parse and the augmentation door still resolving through the drop. Verified in tree: 0005_transcripts_participants.sql:29,33 (drop_relative_path/content_path). Binds: CAP-1, CAP-4, pipeline, api. Prevents: a reviewer reading AD-3 literally and filing a copy-the-video story; a second permanent copy of permanent material
- (decision) AD-17 (new) — every evidence file has a row, no path half data and half code: each file the system stores or serves carries a Postgres row naming its path relative to its root, which root it is anchored to, sha256, byte_size, and the stage that wrote or read it. transcript_source (0005:29-37) is the reference row shape. Composing a served path from a stored value plus a hardcoded filename constant is prohibited — that is how job.drop_path + RECORDING_FILENAME left the recording as the one evidence file with no checksum and no row, so a swapped recording was undetectable where a swapped transcript was not. Binds: CAP-1, CAP-4, pipeline, api, projections. Prevents: two stages inventing different provenance shapes; evidence served without provenance
- (decision) AD-9 amended + deployment envelope corrected: the local-first rule is scoped, not withdrawn — it governs EVIDENCE AND STATE (drops root, content root, Postgres, Neo4j, Meilisearch, publish repo), all of which stay on the dev/demo MacBook, and it does not govern model inference. Model inference may be served by an on-prem LAN host reached over HTTP through the AD-8 ports, bound by providers.<name>.base_url in config.yaml — a config change, never a code change. Three destination classes now: the MacBook, on-prem LAN model hosts, provider APIs. Recorded LAN host: VM 120 cuda-asr on the ThreadRipper/Proxmox box (RTX 4080, CUDA 12.9, nvidia/parakeet-tdt-0.6b-v3 over FastAPI at http://10.77.0.120:8000, ~227x real time, native NeMo timestamps, no diarization). Availability is user-controlled Proxmox scheduling, so it is available infrastructure, not a best-effort dependency (user correction, spec memlog 2026-08-19) — no mandatory local-fallback rule is bound; VM 120 and VM 116 share the 4080 and must not run at once, an operating note for whoever starts VMs. Binds: deployment, adapters, config. Prevents: the spine and config.yaml disagreeing about where inference runs; a LAN GPU service being mistaken for cloud egress
- (decision) Ratification backlog closed in this run (all previously logged as questions, each re-verified in the tree before recording). AD-5 dedup key corrected to mail-else-normalized-display-name: speakers.py:133-165 keys on MAIL_NAMESPACE 'mail:' + case-folded address, falling back to NAME_NAMESPACE + normalized display name; the spine's 'AAD object ID' contradicted shipped code and docs/source-drop.schema.json, which calls Graph/AAD an explicit non-goal. AD-13 gains the moment-identity sentence: transcript_segment.start_ms always carries the PROVIDED transcript's cue timing and the STT lane writes stt_start_ms + alignment_delta_ms in separate nullable columns (0005:190-196), never over start_ms — that is what holds identity_key 'transcript:<start_ms>' fixed when a recording arrives, and therefore what keeps every pre-existing citation resolving. AD-4 gains the augmentation-invalidation trade: augmentation invalidates the meeting's projection state rather than calling unproject_meeting, so the meeting stays answerable from its transcript for the length of the re-run; documented cost is a concurrent rebuild restoring ACTION_NONE, remedied by rebuild --meeting. AD-14 gains the API-distinguishability rule: an augmenting run must be distinguishable at the API from a never-ingested meeting (derivable from job.status + job_stage, no schema change), because the meeting keeps its identity, citations and projections throughout — Epic 2 must not key its empty state on viewable alone. Recovery convention gains emit-order replay: reconstruction replays a meeting's drops in emit order, since an augmenting drop alone is refused by intake
- (event) Update reviewer gate 2026-08-19b: lint PASS (1 pre-existing false positive, REST path {id}). Three lenses — rubric REVISE, web-verification accept-with-fixes, adversarial 2C+4H+5M+1L. Two lenses converged independently on the same defect: AD-17 named transcript_source as the row shape to match while that table has no stage column and stores a BARE FILENAME in drop_relative_path (0005 comment + align.py:199), so 'matched rather than re-invented' would have propagated an anchor that stops resolving the moment an augmenting re-emit repoints job.drop_path at a -002 sibling. Reviews in reviews/review-{rubric,web-verification,adversarial}-update-2026-08-19b.md
- (decision) Gate fixes applied. AD-17: paths are ROOT-relative never nearer-directory-relative (2.1a widens and backfills transcript_source.drop_relative_path); transcript_source is the reference for the path/checksum/size triple ONLY, since it has no stage column, and the producer may instead be determined by a kind/engine pair; a checksum mismatch is read BY ANCHOR — hard stage failure for arrived material (a write-once drop whose bytes changed means AD-1 broke), provenance-not-gate for produced material (ffmpeg is not bit-reproducible, so the new checksum replaces the old); and a served file is resolved by looking its row up from an id, never by joining a client-supplied path onto a root (closes the GET /media/{path:path} hole on branch story/2-1, which streams any readable byte with no row, checksum, or lifecycle check). AD-3: the publish folder is a THIRD CONFIGURED LOCATION and deliberately not a third root — a published artifact is an export the API writes once into a git working tree and no request resolves a stored path against it; storage-layout.md 1 corrected to match, having placed published artifacts under the content root. AD-9: 'never a code change' was false — Stt is Literal[mlx-whisper,parakeet-mlx] with no endpoint slot and no adapters/llm exists, so the claim is narrowed to its true form: a remote engine is a new adapter behind an existing port, no stage/feature-code/spine change, and selecting among IMPLEMENTED engines is the config part. AD-13: the start_ms invariant holds only where the drop PROVIDED a transcript — align.py:542-545 makes STT segments the base when it did not — which is the reason AD-14 admits a recording or a participant graph and not a transcript arriving later. AD-10: env carries secrets AND the two machine-specific root locations, resolving the contradiction AD-3 created with 'secrets only'. AD-15: sourceDeepLink added to the citations array (moment.source_deep_link, 0006:46, UX-DR11) so the transitional no-replay affordance lives in the one citation contract instead of being re-derived by web, evals and search. AD-3 also carries an explicit not-yet-true-in-code marker naming 2.1a
- (question) Gate findings NOT fixed here, routed rather than absorbed. (1) Story-level, for the 2.1b spec not the spine: 2.1b derives sourceId from the video's sha256 but its own matrix allows transcript-only drops, leaving occurrence identity undefined for those — the duplicate-meeting failure UNIQUE(source_id) exists to prevent; and two drop producers answer re-arrival differently (the puller emits a -002 augments sibling, 2.1b reports exists), so a hand-brought video can never gain a transcript, contradicting 2.1b's own Design Notes. (2) Story-level for 2.1a: it records the recording on meeting_media with size_bytes where AD-17 says byte_size — a naming divergence to settle in the story before build. (3) Spine, deferred with a revisit condition: the core-entity ERD omits entities the ADs now name; revisit when Epic 2 touches the schema, since guessing names now would contradict shipped DDL. (4) Adapter-fallback semantics are a real silent dimension — config.yaml has three incompatible precedents (ocr.fallback = construction-time host probe, llm.*.fallback = model id, embedder = deliberately none) — but the user withdrew the demand-a-local-fallback constraint on 2026-08-19, so this stays an open question for whoever adds the first remote engine rather than a bound rule
- (event) spine updated (storage anchors + inference envelope, 2026-08-19): AD-3 and AD-9 amended for the two queued items, AD-17 added, AD-4/AD-5/AD-10/AD-13/AD-14/AD-15 and the Recovery convention ratified against shipped code, solution-design.md and the spec's storage-layout.md aligned. AD IDs stable; AD-17 is the only new one
- (event) Story 2.1 code reviewer (branch story/2-1-review, commit 0aec4d7) escalated get_recording() serving job.drop_path/recording.mp4 as a high-severity AD-3 conflict and asked for enforce / ratify-exception / defer. Answered: the location question was already settled on main at 01c8dfd+ecdbb61 — the reviewer branched before the amendment and read the old one-root AD-3 (verified: its copy still shows the pre-amendment heading). Not an exception to ratify: the two anchors always existed in 0005; AD-3's text was incomplete. But get_recording() remains non-conformant for three reasons the review did not name — job.drop_path is absolute and returned by GET /jobs, recording.mp4 is a code constant composed onto a stored value (AD-17), and the recording has no row, sha256 or byte_size. Conformance work is story 2-1a-evidence-paths-anchored-to-configured-roots on main, NOT the branch's spec-2-1-recording-under-the-content-root, which is withdrawn — treating the latter as required work would build the copy-the-video fix that was explicitly rejected. Also flagged: the review branch marks 2-1-media-streaming-replay-foundation done where main has it in-progress, a sprint-status conflict at merge. glossary.md's Content root entry (cited by the reviewer as authority for video-under-content-root) carried the same one-root error and is corrected here, with a Drops root entry added
- (decision) AD-1 tightened (story 2.1 reviewer decision 2, symlinked source-drop evidence): a drop's canonical files must be REGULAR FILES holding the bytes. Intake refuses a drop whose recording.mp4, transcript.vtt, transcript.txt or metadata.json is a symlink, and refuses a drop directory that is itself one. A hard link is not a symlink and stays permitted — it names the same bytes from inside the drop. Rejected the containment-rule option as self-defeating: if the target must sit under the drops root the symlink buys nothing, and if it may point outside then write-once is unenforceable, AD-17's sha256/byte_size describe bytes that can change without the row changing, and backing up the drops root does not back up the evidence. Rejected deferral: 2.1a is path anchoring, not evidence admissibility, and deferring leaves the shipped split state — domain/drops.py present() uses is_file(), which follows links, so a symlinked recording is admitted, reports has_recording=true, and then 404s at replay, i.e. a meeting claiming replay evidence it cannot serve. Policy decided here; implementation lands in 2.1a, whose matrix and execution list now carry it. Binds: drop contract, intake, worker. Prevents: evidence whose bytes live outside the write-once drop
- (note) Story 2.1a also corrected while open: meeting_media used size_bytes where AD-17 and transcript_source use byte_size — aligned, since 0009 is unwritten and the table name is still free. This closes the naming divergence logged as routed finding (2) in the gate triage
- (event) Rendering pass (2026-08-20): 3-minute AI-cohort demo deck requested — PowerPoint (.pptx, local + committed per no-cloud-artifacts rule) under demo/: title, app overview, architecture diagram, stack+why, storage/RAG decisions, embeddings, ingestion sequence + derived objects, ERD, query sequence, 1-minute demo script. Slide-grade mermaid diagrams derived from the spine's (simplified, not amended); spine content unchanged, no AD touched.
- (event) Update run 2026-08-31: reconcile the spine with the built system. Intent from user: 'make sure the architecture is up to date with the current state of the project.' No spine amendments hiding on branches — story/10-3..10-6 are all 0 ahead of main. Brownfield sweep of server/, config.yaml, migrations 0001-0016, infra/, sprint-status.yaml. Central finding: docs/architecture.md (the in-repo technical contract, 17 ADs condensed) has been kept CURRENT story by story and is AHEAD of this spine on AD-10, AD-5 and the invariants list; the spine is the stale artifact of the two. This run brings the spine to the code, not the reverse.
- (decision) AD-3 and AD-17 amended in place: both 'not yet true in code' markers REMOVED — story 2.1a landed. Verified: migration 0008_drop_root_anchored_paths.sql exists and CHECKs that no stored path is absolute or carries '..'; job.drop_relative_path, transcript_source.drop_relative_path (widened to <drop-dir>/<filename>) and meeting_media.drop_relative_path all hold root-relative values; MM_DROPS_ROOT is real (.env.example:50, README, evals/RUNBOOK.md); 'make backfill-drop-paths' anchors pre-2.1a rows. The rule text is now a description of the code rather than a target it is moving to.
- (decision) AD-10 amended in place — the largest divergence, and story 8-1 is literally named 'AD-10 amendment and binding catalog' (done), with 8-2 persisted-selection done. config.yaml no longer 'drives everything' alone: it declares what is ALLOWED (per LLM role a catalog[] plus a default among them) while a person's PICK among those allowed entries is user-declared data in the api-owned app_setting table (migration 0016), written only by api/settings.py, read by the worker when it resolves a role. Env has also widened beyond secrets+two roots: it now carries the checkout's private-stack name, the generated incarnation id MM_STACK_ID, and the seven store host ports (story 11.2). Binds: all config readers, api, worker, evals. Prevents: a builder putting a user-facing selection into the versioned file, or a second config file for infrastructure location
- (decision) AD-5 amended in place — the table-ownership map gained three groups the spine never listed. API-OWNED: app_setting (0016, story 8.2 — only api/settings.py writes it). WORKER-OWNED and MACHINE-DERIVED: topic + topic_mention (0014, story 10.1) and thread + thread_membership (0015, story 10.2) — navigation metadata, explicitly NOT artifacts: they never enter extracted-approved-published, never get an artifact row. topic rows are replaced wholesale per meeting on an extraction rerun; thread derivation is instead IDEMPOTENT via thread.identity_key because the graph projection, 10.2a curation and 10.3's timeline all key on thread.id. Story 10.2a will add human curation (merge/split/rename) as separate API-owned rows on top, the same split AD-5 already makes for participants
- (decision) AD-9 amended in place — its worked example was inverted by reality. The spine predicted the first remote engine would land on Stt ('the Stt binding names an engine and model with no endpoint, so the first remote ASR engine adds an endpoint'). It landed on Diarizer instead: adapters/diarize/remote_http.py (story 7-1, backlog B-36) posts one multipart upload to POST /diarize on the LAN GPU host at config.yaml diarizer.base_url http://10.77.0.120:8000, stdlib urllib, no new dependency. Stt still has no endpoint slot. AD-9's claim held — a remote engine was a new adapter behind an unchanged port and nothing else moved — so the rule stands and only the example is corrected. Also folded in: story 11.2's per-worktree compose stacks (infra/worktree_stack.py) make 'infra in Docker' one stack PER CHECKOUT, project meetingminer-<slug>, not one global stack
- (decision) NEW AD-18 — Degradation is never silent. The one genuinely new cross-cutting invariant this sweep found, and it passes the two-units test cleanly: an adapter author choosing to substitute a working engine for a failing one, and another choosing to fail by name, are both defensible in isolation and incompatible in aggregate. Owner decision (Tim, standing): a silent fallback is rejected; failures must surface. Realized two ways in code, and the AD must admit both: the remote-http diarizer NEVER substitutes — every failure raises DiarizerError carrying endpoint, model and the host's own reason verbatim, because a meeting ingested with no speaker turns when diarization was asked for is indistinguishable after the fact from a healthy host's empty result; while the LLM port MAY substitute its configured fallback but must REPORT it — LlmReply carries fallback_engaged and the answering model. Binds: every adapter, api, worker, evals. Prevents: a degraded run that reads as a clean one
- (decision) AD-1 amended in place: YouTube is no longer 'future'. Epic 6 landed acquisition as a first-class multi-source surface — 6-2 youtube-acquisition-command, 6-2a playlist-acquisition, 6-3 local-files-acquisition with transcript dialect conversion (server/meetingminer/transcripts/dialects.py), 6-4 acquisition-launch-surface (/acquisitions routes), 6-6 youtube-deep-links — all done. The drop contract absorbed them without change, which is AD-1 working. One consequence to record: config.yaml acquisition.youtube.max_duration_minutes is a refusal boundary the loader requires with no fallback, an instance of the 'every threshold is configuration' invariant
- (constraint) Structural Seed correction — the spine's deployment paragraph asserts of VM 120 cuda-asr: 'The deployed service exposes transcription only — its NeMo install carries diarizer code but no weights and no endpoint — so it does not repair the speaker-less archive VTTs.' That is now FALSE. The host serves POST /diarize, config.yaml binds diarizer.engine: remote-http to it, and a dated owner note in the file records the measurement that settled it: 60 minutes of audio in 57.5s there versus 35m51s for in-process pyannote on this Mac, 37.4x apart. Availability is still an operator scheduling decision (VM120 onboot=0, shares its GPU with VM116)
- (constraint) Source-tree and component seed are missing four shipped server modules: digest/ (the Morning Digest example generator — read-only over artifact/meeting, no migration; it was listed under Deferred as 'build only if time permits' and has since been built, so it must move out of Deferred), prune/ (delete every meeting outside an explicitly named keep-set; refuses a bare invocation, reports by default and deletes only under --delete — the corpus-removal path that exists because the corpus was rebuilt onto an owned sandbox), publish/export.py, and transcripts/dialects.py (story 6.3)
- (version) Stack table refreshed against the actual tree rather than re-asserted (seed: the code owns this now). pyproject: Python >=3.12,<3.13; fastapi >=0.141,<0.142; neo4j driver >=6.0,<7 (never listed before); litellm >=1.60 (spine said >=1.97); meilisearch SDK >=0.43,<1; parakeet-mlx >=0.3 (spine said 0.5.x); pytest >=9.1,<9.2. compose images are digest-pinned: pgvector/pgvector:pg18, neo4j:2026.07-community, getmeili/meilisearch:v1.53. web: react ^19.2.8, vite ^8.2.0, typescript ~6.0.2 (spine said 'current'), @hey-api/openapi-ts ~0.99.0
- (event) Sweep scope recorded for the next resume: 68 story items done, 8 in-progress epics (6,7,8,10), epic-11 done including 11-2 per-run store isolation. Landed since the spine was last distilled: migrations 0009 artifacts, 0010 extraction_sources, 0011 artifact_publish_metadata, 0012 extraction_prompt_hash, 0013 series_projects_products, 0014 topics, 0015 threads, 0016 app_setting
- (direction) Two architecture artifacts exist and must not diverge again: this spine (planning-artifact, AD IDs are the citable handles) and docs/architecture.md (the in-repo technical contract builders actually read, which condenses the same numbered ADs). docs/architecture.md was found CURRENT — it already carries the AD-10 catalog/selection split, the env widening, and an Invariants section holding 'Fail closed, fail named, fail before writing' and 'Never guess'. So this update moves the spine to meet the repo doc, and AD-18 is the spine catching up to an invariant the repo doc had already written down informally
- (event) Update reviewer gate 2026-08-31: lint_spine.py PASS — 1 low finding, the same pre-existing false positive as 2026-08-19b (the REST path token {id} in /meetings/{id}/moments read as an unfilled template token). Adversarial + web-verification lenses run inline rather than as subagents (session directive: no subagent dispatch without an explicit ask); scaled to an update rather than a first distillation
- (decision) Three gate fixes applied, each a case where two units could obey every AD and still diverge. (1) AD-4 absorbed a clarification the CODE was already carrying locally — projections/graph.py:40 reads 'AD-4, as clarified for story 10.2', meaning the build had made a decision the spine never recorded: machine-derived navigation metadata is a THIRD projection case outside the publish gate. Topic nodes project with their meeting's evidence and never go through assert_publishable (they carry no lifecycle state to check); Thread nodes are upserted like Screen because a thread spans meetings and a per-meeting pass must never delete one; and a meeting whose topics are not yet threaded projects Topic and no Thread, which is a valid intermediate state, not a fault. Without this an independent builder would plausibly have gated topics like artifacts. (2) AD-8's port list still read 'Diarizer (noop default | pyannote)' — corrected to noop | pyannote | remote-http. (3) AD-10 now names domain/model_selection.py as the single owner of the app_setting key spelling llm.role.<role>.binding: the table is a generic key/value store by design, so no schema constraint can distinguish a valid key from a typo, and the owning module is what stands in for one — otherwise the api writer and the worker resolver could spell the same setting differently and each be internally correct
- (event) spine updated — 18 ADs (AD-18 added, AD-1/3/4/5/8/9/10/17 amended in place, no ID renumbered or reused). Deferred lost the Morning Digest entry (built). Paradigm line corrected from two model-writable surfaces to three
- (event) Update complete 2026-08-31. docs/architecture.md brought into step in the same pass so the two never diverge again: it gained AD-18, the 10.2-landed thread paragraph replacing 'nothing projects them yet', the remote-http payout and per-checkout stacks in AD-9, and app_setting + the machine-derived tables in AD-5; its count line moved from Seventeen to Eighteen. Lint re-run: PASS, same single {id} false positive. Both documents now carry 18 ADs.
- (decision) AD-4 amended for Epic 12 pre-check: 'rebuild writes no primary data' is now STATED, not merely implied. The spine only said rebuild regenerates the stores 'from Postgres + config.yaml alone' — an input claim a builder could satisfy while also writing rows. Commit ad43886 got the separation right independently (make threads is its own command, not part of rebuild) but it was reasoning, not a rule, and the next builder could have folded a derivation in. Now fixed: a derivation that mints primary rows is never folded into rebuild, which fixes the order ingest -> derive -> rebuild, because graph.py:456 projects topic.thread_id as it finds it and a rebuild run first bakes nulls in and reports success. The Recovery convention gained the same ordering — it named only 're-ingest drops, then rebuild', which would restore a corpus with no threads
- (decision) AD-6 amended for Epic 12 story 12.2: a meeting-scoped artifact IS admissible — nothing in the spine forbade it, and the moment-anchoring was ERD seed (which the code owns), not an invariant. Scope is a property of kind, declared in exactly one place so no reader re-derives the mapping. But the CITATION CONTRACT DOES NOT WIDEN, and this is where the ruling departs from the story text as drafted. 12.2 says a summary 'carries the meeting it summarises as its citation'; that is refused. meeting_id is scope and provenance, never a citation, because AD-15's array carries startMs/endMs and admitting a momentless entry hands the web replay links, the eval checks and search a citation that cannot replay — the silent degradation AD-18 forbids. The story's own second clause is the right answer: a summary is citable through the moments its individual claims anchor to, and an unanchored claim is not citable, which is the rule every claim already obeys. Also pinned: widening scope must not weaken the anchor — where a moment is named, the composite edge to that moment's own meeting still holds
- (question) OPEN for the owner, story 12.1: should an ADOPTED extraction document's text be copied into Postgres? Generated documents are settled — Postgres, primary data: they are model output with no other home, they are not binaries (AD-3's content root is for frames/screenshots/audio), and artifact.body already sets the precedent for extraction text in a column. But 12.1 says 'both paths store it', and for an adopted document the bytes are already permanent in a write-once drop with extraction_source.drop_relative_path pointing at them — so storing the text again is a second permanent copy of permanent material, which is the exact reasoning AD-3 used to refuse copying the recording under the content root. Counter-argument, which may well win: AD-3's anti-copy reasoning was about a large binary where a copy doubles real storage, and a few KB of markdown is different in kind, while one uniform read path for the new endpoint is worth something. Not decided unilaterally — if the copy is kept, the spine should say why AD-3's reasoning does not extend to small text, so the next reader cannot cite AD-3 to reverse it
- (event) Epic 12 pre-build spine check done at peer request (session meetingminer-e8), before any 12.x story starts. Verified against the tree rather than the story text: artifact.moment_id is NOT NULL with a composite (moment_id, meeting_id) FK (0009:17,35); extraction_source stores sha256/byte_size and a NULL drop_relative_path for generated documents (0010:29-36); graph.py:456 reads topic.thread_id. Lint after amendment: PASS, same single {id} false positive
- (decision) RESOLVED, reversing my own recommendation: an adopted extraction document's text goes in Postgres too — 12.1's 'both paths store it' was right as drafted. Owner requirement settled it (all extraction documents must be searchable and indexed), and the mechanism is stronger than the uniform-read-path argument that had been offered for it. Verified: projections/ never opens an evidence file — its only filesystem touches are its own config.yaml (cli.py:51) and a tempdir lockfile (locks.py:87,157) — and every indexed field is a Postgres value (search.py:78 moment.text, :85 screenshot.ocr_text, :129 chunk.text). AD-4 has rebuild regenerating both stores from Postgres + config.yaml alone, so text living only in a drop file cannot be indexed without making projections a filesystem reader, and would fall out of search on every rebuild. My AD-3 analogy was wrong on its own terms: the recording is never indexed, and everything searchable derived from it is already materialized as rows — so the recording precedent supports copying searchable text into Postgres rather than forbidding it
- (decision) AD-3 amended with the line the recording precedent actually draws: the anti-copy rule governs material the system SERVES BUT DOES NOT RETRIEVE OVER. Where content must be searchable it is a Postgres row, not a file, because the projections are built from Postgres + config.yaml alone (AD-4) and never open an evidence file. Explicitly warns the next reader not to read the recording precedent as a general rule against copying arrived material into Postgres — it is a rule about material nothing searches. This closes the open question from the Epic 12 pre-check
- (question) OPEN and genuinely the owner's, raised by the second requirement (users must be able to ask questions about the documents): does an extraction document become retrievable in CHAT, and if so does it pass the publish gate? Two collisions, both real. (1) AD-4's consequence is that search and chat operate over evidence plus PUBLISHED artifacts only, and publish_gate.py enforces it at the one Postgres read (published_artifacts selects WHERE state = 'published'). An extraction document is unapproved model output, so indexing it as-is puts draft model prose into chat retrieval — precisely what the gate exists to stop. (2) chunking.py gives a chunk the UUID of its FIRST TRANSCRIPT SEGMENT as its identity (AD-6); a document chunk has no transcript segment, so under the current identity rule it cannot get an id and therefore cannot be cited. Note the scope is narrower than it first looks: the document's substance already becomes citable artifacts (adr/action-item, and 12.2's summary), so the only genuinely new retrievable material is the residue 12.1 wants kept — prose the parser ignored. And an extraction document is not evidence, it is a CLAIM ABOUT evidence: citing it would prove that the model said something, not that the meeting did, which is the circularity AD-6 exists to prevent
- (decision) OWNER RULING 2026-08-31 (asked, options laid out with the risk named, owner chose the widest): extraction documents are indexed IMMEDIATELY and UNGATED — search and chat retrieve over evidence, published artifacts, and every extraction document regardless of approval state. This is a deliberate widening of AD-4's 'evidence plus published artifacts only', and the first exception to the publish gate in the capstone. Owner's reasoning stands on 12.1's own motivation: the run whose text somebody needs to read is exactly the run that yielded nothing worth approving, so gating documents behind approval would withhold them in precisely the case they exist for. AD-4 amended to carry the exception explicitly rather than leaving publish_gate.py's comment as the only account of a rule that no longer holds. Two constraints attached without re-asking, because both follow from invariants already in force rather than from the choice: (1) the exception is to REACH, never to legibility — AD-18 forbids unreviewed output reading the same as reviewed output, so a document carries its unreviewed machine-written status in the indexed record and every rendering surface labels it, the same requirement topics and threads already carry; (2) a document is never a citation target, because it is a claim ABOUT evidence rather than evidence, and citing it would establish that the model said something rather than that the meeting did — its content reaches an answer only through the moments its claims anchor to. Also pinned: a document takes no chunk identity from a transcript segment (chunking.py keys chunks on their first segment's UUID and a document has none); its indexed identity derives from extraction_source, and since it is not citable that identity is a build decision, not an invariant
