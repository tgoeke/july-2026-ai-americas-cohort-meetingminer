---
topic: MeetingMiner capstone — evidence-first meeting mining (brief + agentic-rag & eval-strategy brainstorm intents)
updated: 2026-08-29T12:38
---

- (direction) Create from 3 sources: tim.Blake-capstone-product-brief.md + brainstorm-intent (agentic-rag) + brainstorm-intent (eval-strategy); slug=meetingminer (obvious from project); express distill, rich input
- (capability) CAP-1 evidence ingestion: Teams recap URL (puller script) or local recording -> precomputed evidence bundle (screens, speaker transcript, moments, alignment, provenance, replay links, MS Graph participants); success = 100% capture recall on unseen scripted demo
- (capability) CAP-2 evidence domain graph: Moment atomic object; Meeting/Screen/Screenshot/Project/Product/Participant/artifacts graph IS the GraphRAG index; screen lineage across meetings; success = lineage + Rowan traversal queries
- (capability) CAP-3 search + cited Q&A: corpus search by meeting/topic/mention; NL questions via GraphRAG; every answer cites moments; success = citations replay evidence, timestamp within 15s of script
- (capability) CAP-4 moment view + replay: screenshot top, transcript below, right-rail analytics, full A/V replay; drill-down with highlighted mentions, inline replays, screenshot series; success = seconds-fast verification
- (capability) CAP-5 extraction: ADRs + action items from moments; prompts baked in, visible in UI, config-swappable; success = fuzzy set-match found/missing/extra + LLM/human judge
- (capability) CAP-6 human-approved publishing: artifacts start unpublished; per-moment approval publishes to folder + commits ADRs to plain local git; success = nothing publishes unapproved
- (capability) CAP-7 eval harness: YAML scripted ground truth; deterministic-first tiered judging (asserts -> LLM judge -> human runbook); success = one full run before demo, capture recall 100%, over-capture guardrail holds
- (constraint) No citation no answer — every factual claim about meeting content traces to a moment, no exceptions (user could imagine none)
- (constraint) Deterministic core owns capture/alignment/provenance/replay/search/eval; AI contributes evidence, never owns truth
- (constraint) All model interaction behind adapter interfaces, swappable via configuration
- (constraint) Bias to preserve evidence over dedup; 100% capture recall required; over-capture guardrail bounds spam
- (constraint) One-way generation engine: no status sync back; MM never owns artifact lifecycle state (Asana/Linear/RAID do)
- (constraint) Series membership human-declared, never inferred; recurring meetings get per-meeting docs
- (constraint) AI proposes humans approve — publishing is a per-moment user gesture
- (constraint) Sequencing: eval harness + setup complete BEFORE demo-script work begins
- (constraint) Ingestion fully precomputes before viewing
- (constraint) Capstone reality: solo dev, demo ~1 week (~2 weeks total); design everything, build the slice
- (decision) Non-goals: autonomous ingestion/notifications/digest delivery; self-assembling decks; stakeholder agree/disagree; lifecycle tracking; outbound routing to live systems; retrieval-eval implementation (document only); image-similarity dedup scan; auth/enterprise (Clerk/Entra)
- (decision) Companions (spec-authored): scope.md (MoSCoW + retrieval slice + product-later), ux-spine.md, eval-strategy.md, architecture-diagrams.md (diagram rule)
- (assumption) MS Graph API access available at ingest for participant pull
- (assumption) Capstone is local-first, single-user, no auth (Clerk/Entra explicitly product-later)
- (assumption) Teams recap puller = manual paste-URL local script, not an integration
- (question) PNG-to-slide matching method for capture-to-script OCR matching is TBD (eval build plan)
- (event) Self-validate pass 1 (coherence): PASS — all 7 CAPs have intent+success; constraints all rule something out; 7 non-goals; success signal testable against scripted ground truth; IDs CAP-1..7 fresh and unique
- (event) Self-validate pass 2 (preservation): PASS — brief, agentic-rag intent, eval intent walked claim-by-claim; all load-bearing claims land in SPEC.md or companions. Wrapper-only content dropped: brief tagline/exec-summary rhetoric, corp/Rowan/2024 anecdote narrative detail (gist kept in Why), labor-chain prose (motivates CAPs already present), brainstorm session metadata
- (event) Spec rendered: SPEC.md + companions scope.md, ux-spine.md, eval-strategy.md, architecture-diagrams.md; sources fully absorbed: product brief + both brainstorm intents
- (direction) User: the eval runbook must be really good — elevate it from a build-plan bullet to a first-class deliverable
- (capability) CAP-8 eval runbook: written runbook covers the FULL eval run (setup, deterministic asserts, LLM-judge review, human judging, verdict recording, failure triage, rerun); success = an operator executes a complete eval run end-to-end following only the runbook
- (assumption) Interpreted 'eval runbook' broadly: operates the entire eval run, not only the human-judging tier (broader reading subsumes the prior human-judging runbook)
- (decision) New spec-authored companion eval-design.md: ground-truth YAML schema, per-check algorithms/thresholds, judging tiers, full eval runbook (CAP-8), cadence, retrieval eval designs (DOCUMENT-only items included)
- (decision) Design calls authored in eval-design.md (user-requested creation, not source-derived): OCR-anchor matching >=0.8 for capture recall, dedup candidate similarity >=0.9, action-item fuzzy match >=0.75, run folders evals/runs/<run-id>/ immutable after verdict, rerun-on-any-pipeline-change rule, human-verdict-wins escalation
- (question) Confirm thresholds (0.8 anchor match, 0.9 dedup, 0.75 action-item) and OCR engine / LLM judge model picks before harness build
- (event) Self-validate after update: PASS — CAP-8 has intent+success; IDs stable CAP-1..8; eval-design.md cited from CAP-7/CAP-8; eval-strategy.md build item updated to point at runbook; no source claims displaced
- (decision) User accepted authored thresholds (anchor 0.8, dedup 0.9, action-item 0.75) as provisional baselines for harness build; explicitly to be re-evaluated during eval runs — resolves the threshold-confirmation open question; OCR engine + LLM judge model picks remain open
- (direction) User update: local (non-Teams-URL) ingestion may pair a video with a provided transcript file; retrieval becomes multi-store (indexed full-text doc search + GraphRAG); derived artifacts (ADRs, action items, arch docs) re-indexed into retrieval
- (capability) CAP-1 amended: local ingestion accepts a recording alone or a recording+transcript pair; a provided transcript is treated like the recap transcript (verified/merged, original never erased)
- (capability) CAP-9 multi-store retrieval: indexed full-text search engine over evidence documents alongside the GraphRAG domain graph; published artifacts re-indexed into the stores as searchable citable knowledge; success = one query hits both stores (moment via doc index, published ADR via re-index) with replaying citations, and unpublished artifacts never surface
- (constraint) Only published (human-approved) artifacts enter retrieval stores — search/Q&A never surfaces unapproved AI output (user chose on-publish indexing over on-generation)
- (decision) Full-text document index is capstone BUILD scope (user chose build over design-only) — joins Cluster C slice; scope.md updated
- (assumption) CAP-9 success wording authored from the two user decisions (dual-store hit + unpublished exclusion), not user-dictated verbatim
- (question) Which eval checks cover the full-text doc index and artifact re-indexing (CAP-9)? Retrieval eval is documented-only; CAP-9 is now build scope
- (event) Self-validate pass 1 (coherence) after multi-store update: PASS — CAP-9 has intent+success, intent stays WHAT (no engine named), new publish-gate constraint rules out indexing drafts, IDs stable CAP-1..9
- (event) Self-validate pass 2 (preservation): PASS — all 4 user claims landed (video+transcript pair -> CAP-1; multi-store -> CAP-9; doc search index -> CAP-9/scope.md; artifact re-index -> CAP-9/constraint/scope.md/diagram); no wrapper-only drops
- (event) Spec re-rendered: SPEC.md (CAP-1 amended, CAP-3 cross-ref, CAP-9 added, publish-gate constraint, new open question), scope.md Cluster C expanded, architecture-diagrams.md multi-store + re-index loop
- (direction) User: domain objects (hierarchy + moment/artifact model) should persist in a database while artifacts are built up, backing the search engine
- (constraint) Database of record: domain objects and artifacts persist in a database from creation; retrieval stores are derived, rebuildable projections of it, never the primary copy; unpublished artifacts exist only in the database of record
- (capability) CAP-2 amended: domain graph is persisted in the database of record (artifacts included while unpublished); GraphRAG traverses the persisted graph. CAP-9 amended: both retrieval stores are projections of the database of record
- (assumption) 'Derived, rebuildable projection' articulation authored from user direction; DB technology deliberately unnamed — architecture decides
- (event) Self-validate pass 1 (coherence) after database-of-record update: PASS — CAP-2/CAP-9 intents stay WHAT (no DB tech named), new constraint rules out stores-as-primary and drafts-in-stores, IDs stable CAP-1..9
- (event) Self-validate pass 2 (preservation): PASS — user claim (persist domain objects + in-progress artifacts in a database backing the search engine) landed in CAP-2, CAP-9, constraint, and both diagrams
- (event) Spec re-rendered: SPEC.md (CAP-2 persisted graph, CAP-9 stores-as-projections, database-of-record constraint), architecture-diagrams.md (Domain Database inserted, re-index loop routed through DB)
- (direction) User accepted all four open-question recommendations (2026-08-17), with caveat: judge model chosen via graded bake-off — frontier APIs (Anthropic, OpenAI), local Ollama on dev Mac (M4 Max, 128GB unified RAM), and hosted open-weight frontier models (e.g. Kimi)
- (decision) PNG-to-slide matching resolved: script-authoring rule — every scripted slide/screen carries a unique distinctive ocr_anchor (planted if not natural); image hashing product-later for unscripted recordings
- (decision) OCR engine: Apple Vision framework primary (macOS demo), Tesseract portable fallback, both behind the same swappable interface
- (decision) LLM judge chosen by bake-off: candidates judge the same scripted-corpus sample per rubric; graded on agreement with human verdicts; winner pinned (exact model id) and recorded in run metadata; cloud judges receive derived data only per egress allowlist
- (decision) Judge-model change triggers the rerun rule (invalidates prior verdicts), same as pipeline changes — runbook amended
- (decision) CAP-9 eval coverage: two new BUILD checks — doc-index recall@k on planted phrases (2.10) and deterministic publish-gate asserts (2.11: published artifact present in both stores, unpublished in neither) — resolves the CAP-9 eval open question
- (assumption) Authored calls in bake-off/check design: grading = human-verdict agreement, recall@k with k=5 provisional, candidate model list illustrative not binding
- (note) Both SPEC-level open questions now resolved; remaining pre-build work items (bake-off run, harness) tracked in eval-design.md not as spec questions
- (event) Self-validate pass 1 (coherence) after open-question resolution: PASS — no dangling TBDs; checks 2.10/2.11 have tier/algorithm/threshold; bake-off section states candidates, method, grading, pinning; rerun rule consistent across sections 4.7/6/7; IDs stable CAP-1..9
- (event) Self-validate pass 2 (preservation): PASS — all user caveats landed: bake-off with graded judging, frontier APIs (Anthropic/OpenAI), local Ollama on M4 Max 128GB, hosted open-weight (Kimi et al.) all in eval-design section 7
- (event) Spec re-rendered 2026-08-17: SPEC.md open questions cleared; eval-design.md (authoring rule, 2.1 resolved, checks 2.10-2.11, section 7 bake-off, runbook + rerun rule + cadence amendments); eval-strategy.md TBD line cleared
- (direction) User (2026-08-18): M365 developer sandbox discontinued — no dedicated Teams test tenant. New plan: host scripted mock meetings on the corp Teams tenant, pull with the existing pull_transcript puller script, drop output into a folder on the dev Mac; the many already-pulled real meetings in pull_transcript/ are also available
- (decision) MS Graph dropped from CAP-1 (no app registration on corp corporate tenant): participants derive from transcript speaker names + _source.json provenance sidecar; Graph participant pull moves product-later — reverses the Graph-access assumption
- (decision) Ingestion entry point = drop folder of puller output (recording + transcript + provenance sidecar, <Title>/<M.D.YY>/ layout); the paste-URL flow lives in the puller script outside MM — CAP-1 amended, MM never talks to the tenant
- (decision) Real pulled meetings (vendor/project/Boomi/corp) join the DEMO corpus alongside scripted mocks (user accepted client content visible in demo); eval + 100% capture-recall ground truth remain scripted-only — real meetings have no ground-truth manifests
- (constraint) No dedicated Teams test environment: scripted mock meetings are hosted and recorded on the corp production tenant, retrieved by the puller logged in as the user — rules out tenant-side automation, Graph app registration, and any MM-to-Teams integration
- (assumption) Puller output layout is the ingestion input contract: <Title>/<M.D.YY>/ folder with transcript .txt, recording .mp4, original transcript exports, _source.json sidecar (authored from pull_transcript README/CLAUDE.md, not user-dictated)
- (note) Reconciled with planning-artifacts/epics.md (AD-1): drop-directory schema is an architecture decision — assumption softened to recording + transcript export + provenance/metadata sidecar; puller-side best-effort Graph lookup stays outside MM (sidecar carries whatever participants the puller supplies); epics.md line 88 anticipated this exact SPEC update
- (event) Self-validate pass 1 (coherence) after corp-tenant/drop-folder update: PASS — CAP-1 intent stays WHAT (drop schema deferred to architecture), new no-test-tenant constraint rules out tenant automation/Graph app/MM-Teams integration, Graph non-goal added, Graph assumption removed, IDs stable CAP-1..9, open questions still none
- (event) Self-validate pass 2 (preservation): PASS — all 5 user claims landed (sandbox discontinued -> constraint; corp-hosted mocks -> constraint/scope/ux-spine; puller grab -> CAP-1/assumption; folder drop -> CAP-1/diagram; pull_transcript corpus -> scope.md Corpus) plus 3 elicited decisions (participants, demo-corpus role, folder entry point); no wrapper-only drops
- (event) Spec re-rendered 2026-08-18: SPEC.md (CAP-1 drop-folder ingest, no-test-tenant constraint, Graph non-goal, assumptions reworked), scope.md (Corpus section, Graph product-later), ux-spine.md (ingestion flow), architecture-diagrams.md (Drop Folder source node)
- (event) User confirmed all 3 assumptions (local-first/no-auth, pull_transcript puller, AD-1 drop contract) 2026-08-18
- (note) msft-sandbox-setup-guide.md removed (git rm) — superseded by corp-tenant plan; ARCHITECTURE-SPINE.md still lists it in sources: and cites 'sandbox tenant / Graph egress' in Structural Seed — needs a bmad-architecture update pass
- (direction) User (2026-08-18): refresh spec companion(s) against the amended ARCHITECTURE-SPINE.md (the bmad-architecture update pass the earlier note called for has landed)
- (decision) Spine AD-1 adopted at spec level: transcript-only drops are first-class (recording and/or transcript; most real pulled corpus has view-only recordings) — CAP-1 amended; transcript-only moments carry no screenshot and no video replay
- (decision) Ingestion trigger per spine AD-14: puller notifies MeetingMiner after finalizing a write-once drop; files landing in the folder alone never ingest — CAP-1, ux-spine flow, diagram note amended
- (decision) Sidecar naming per spine AD-1: provenance (_source.json content) is embedded in the metadata sidecar, not a required standalone file — diagram + assumption wording refreshed
- (note) scope.md Corpus: real pulled meetings mostly ingest transcript-only — searchable and citable, without screenshots or video replay
- (event) Self-validate pass 1 (coherence) after spine-refresh: PASS — CAP-1 intent stays WHAT (no schema/endpoint named), success unchanged and still scripted-testable, IDs stable CAP-1..9, no constraint contradicted (eval corpus remains scripted+recorded)
- (event) Self-validate pass 2 (preservation) vs amended spine: PASS — transcript-only first-class, write-once drop, embedded provenance, notify-not-watch trigger all landed; spine-owned mechanics (metadata.json fields, atomic finalize, sourceId conflict-reject, participant normalization/alias, backup rules) deliberately not duplicated into the spec layer
- (event) Spec re-rendered 2026-08-18 (spine refresh): SPEC.md CAP-1 + drop-contract assumption, architecture-diagrams.md drop-folder node + transcript-only note, ux-spine.md ingestion flow, scope.md real-corpus bullet
- (note) Check-in 2026-08-18: reviewed spike-cluster findings at /Volumes/nvmepool/mm_current/mm (CORPUS-FINDINGS.md, SPIKES.md, spike-results/S-0.md, VIDEO-GAP-REPORT.md, GRAPHRAG-INTEGRATION.md) — measured, not inferred
- (question) DIVERGENCE: spike docs cite a SPEC.md with CAP-1..CAP-11 (CAP-5=crop, CAP-9=ASR, CAP-10=merge, CAP-11=participant graph) and an ARCHITECTURE-SPINE.md with AD-4 Parakeet/AD-6 demux-Tier-1/AD-11 local-server-viewer/AD-13 orchestrator-at-recording/AD-14 no-cloud-egress/AD-15 pip-install-no-Node — none of which are this repo's SPEC (CAP-1..9) or spine (AD-1..16, AD-2 Postgres, AD-9 Docker, AD-12 egress unrestricted). Two lineages share filenames; which is canonical is unresolved
- (note) Measured corpus reality (2026-08-18): pull_transcript holds 28 occurrences, only 8 with .mp4; 20 transcript-only — confirms today's CAP-1 transcript-only-first-class amendment against real numbers
- (note) Measured media: primary NDA demo is 117.6 min 1920x1080 mono (not the 60-90 min bar); source is 16 fps not 30; full PyAV decode+analysis of the 61-min recording ran in 17 s (211x realtime) — capture cost is not a constraint
- (note) S-0 measured over-capture at 0.86 captures/min on real content at the recommended gate; eval-design 2.2 fails above 1.0/min (ceil(duration_minutes)) — only 14% headroom, and the next-looser gate measured 1.33/min = fail
- (note) Capture recall still has no denominator: S-6 independent labeling not run, scripted mock lab unbuilt; S-0 warns a contact sheet built from the extractor's own output cannot measure recall by construction — CAP-7 success (100% capture recall) is currently unmeasurable
- (note) Speaker labels are inconsistent within a single transcript file (Blake, Cameron vs Cameron Blake; bare Avery/Jordan/Jamie; Speaker 8 placeholder) — naive string keying double-counts people and mis-attributes quotes, breaking the who-said-it half of no-citation-no-answer; spike rule is never guess (unresolved/ambiguous beats confident-wrong)
- (direction) User (2026-08-18): the spike cluster at /Volumes/nvmepool/mm_current/mm is a deliberately parallel track — this repo's SPEC (CAP-1..9) and ARCHITECTURE-SPINE (AD-1..16) are canonical; roll in their critical and load-bearing findings only
- (decision) Speaker-name discrepancies traced to dual transcripts (Teams + Krisp/third-party) in the same occurrence dir, not to Teams itself: Teams supplies real Last, First names. Teams is the sole go-forward transcript source; no further double-counting expected on new pulls
- (constraint) Legacy third-party transcript parsing stays required despite Teams-only go-forward: the two NDA demo recordings are the only long videos with full transcripts (the primary capture-eval assets) and carry the legacy pipe format — dropping the parser strands the eval corpus
- (constraint) Speaker attribution never guesses: an unresolvable label stays unresolved and an ambiguous one stays ambiguous, because a wrong attribution is worse than an absent one — who-said-it is half of no-citation-no-answer
- (constraint) Capture-recall ground truth is produced independently of the extractor, never derived from its own output: a set built from what the extractor emitted cannot reveal what it missed, so a self-derived denominator reports 100% while measuring nothing
- (constraint) Evidence capture is designed against ~120-minute recordings (primary real sample 117.6 min, 1920x1080, 16 fps, mono) — the earlier 60-90 minute working bar is below real content
- (note) Story 1.4's own live-stack verification captured 188 screenshots from a 57-min meeting = 3.3/min, against the eval-design 2.2 guardrail fail line of 1.0/min and the spike's tuned 0.86/min — corroborates S-0's uncropped-signal prediction; retune is story-level work, tracked in capture-measurements.md
- (decision) New spec-authored companion capture-measurements.md holds the measured corpus/media/capture-path numbers distilled from the parallel track, so CAP-1/CAP-7 and the eval harness cite measurements instead of assumptions
- (event) Self-validate pass 1 (coherence) after parallel-track rollup: PASS — IDs stable CAP-1..9, no capability added or renumbered; 4 new constraints all bend decisions (independence rule rewrites eval-design 2.1, ~120 min bar changes sizing, Teams-only+legacy-parser rules out dropping a parser, never-guess rules out fuzzy best-match attribution); kernel stays WHAT with companions cited by name; non-goals and success signal unchanged
- (event) Self-validate pass 2 (preservation) vs the 5 parallel-track docs: PASS — corpus/media/transcript/participant-graph facts landed in corpus-facts.md, S-0 capture measurements in capture-measurements.md, GraphRAG schema+constraints in retrieval-prior-art.md, contradictions C1/C2/C3/C7/C8 resolved into constraints or already held. Deliberate drops (their-track-only, logged not silent): LAN machine inventory and IPs, PyAV block-grid config constants tuned to their algorithm, per-owner recording tables in the video gap report, the spike plan itself, their repo-hygiene warnings, Kuzu/Node implementation specifics generalized for this lineage's Postgres
- (event) Spec re-rendered 2026-08-18 (parallel-track rollup): SPEC.md 4 constraints + 3 companion citations, new companions corpus-facts.md / capture-measurements.md / retrieval-prior-art.md, eval-strategy.md capture notes + guardrail headroom, eval-design.md 2.1 independence rule + 2.2 measured headroom, scope.md corpus numbers
- (decision) Story 1.11 Screen Capture Retune added to epic 1 (epics.md + sprint-status.yaml, backlog): crop-before-detection, emit-on-settle, brightness/saturation view classification, tag-never-drop transitions, guardrail under 1.0/min re-verified on the 57-min meeting, all thresholds in config.yaml. Scheduled before 1.6 since moment identification consumes these screenshots
- (decision) epics.md story 1.5 reconciled to the amended SPEC: AAD-object-ID dedup removed (Graph is a non-goal, so no directory identifier exists; participant-graph logins are employee numbers), roster-scoped name normalization kept, never-guess AC added, and a two-lineage transcript-parsing AC added (Teams source of record + legacy format for the eval assets, +-2s second-precision anchors, MM:SS->HH:MM:SS by field count, vtt not a substitute)
- (decision) epics.md story 1.7 gained the five upstream-proven store constraints (single writer, insert-only vectors, recorded embedding model, structural indexing without the model host, meetingId scoping) plus never key projections on a sequence number
- (question) Transcript-only replay is contradictory across live sources: SPEC CAP-1 says transcript-only moments carry no screenshot and no video replay, and epics.md story 1.6 says no replay link is produced — but the drop sidecar carries the Stream URL, which the parallel track identifies as exactly what makes replay possible for transcript-only occurrences (a deep link back to the source, since there is no local media to seek). Unresolved: does a transcript-only moment get a source deep link, or no replay affordance at all
- (event) Builder handoff prompt written for story 1.5 at _bmad-output/implementation-artifacts/build-prompt-story-1-5-2026-08-18.md — points bmad-build-auto at corpus-facts.md sections 3-4 as established input properties, names the two amended SPEC constraints, flags the AAD removal so it is not reintroduced, and fences scope away from screens/1.11 and pull_transcript
- (event) Builder handoff prompt written for story 1.11 at _bmad-output/implementation-artifacts/build-prompt-story-1-11-2026-08-18.md — runs concurrently with 1.5 (different stages, different config blocks); verification is the 57-min re-run under the guardrail plus human review of removed captures, since recall has no denominator until story 5.1
- (event) SPEC.md Open Questions re-derived 2026-08-18: section claimed 'None' while the memlog carried the live transcript-only replay question — now rendered as an open question, with the 2026-08-17 resolved set kept as a closed line
- (event) Self-validate (scoped to the Open Questions re-render): PASS — capability IDs untouched (CAP-1..9), no kernel field altered, companions list unchanged; no companion carried a contradicting question state
- (note) Reviewed live pull_transcript copy at /Volumes/nvmepool/mm_current/pull_transcript 2026-08-18 (user-provided): org-chart backfill, Kuzu GraphRAG store, and an embeddings bake-off — measured from the files on disk, not from prose
- (note) GraphRAG store contents now measured (retrieval-prior-art 5 previously recorded them as unknown): 167 meetings, 7730 chunks, 150 people, 41 decisions, 175 actions, 97 risks, 80 topics, 19.6k SPOKE / 21k MENTIONS / 189 EVIDENCE edges, 217 MB on disk
- (note) The 167 meetings reconcile as 28 occurrence .txt transcripts plus 141 archive .vtt files — the archive TRANSCRIPTS have been fetched into the Recordings and Transcripts mirror even though 0 .mp4 have; corpus-facts 1 recorded only the unfetched video count
- (note) Chunking measured: whole speaker turns packed to ~1400 chars with one-turn overlap, chunk id is meetingId#seq, carrying startSec/endSec/tsLabel/speakers — turn boundaries preserved because speaker attribution and [m:ss] citations both hang off them
- (note) Embeddings bake-off (2026-08-18, upstream): 9 configurations of 8 local models over 303 labelled tasks against the full 7730-passage corpus; two query sets biased in opposite directions on purpose — 183 cited-claim queries (reuse transcript wording) and 120 paraphrased natural questions (distinctive words removed)
- (constraint) Keyword search carries the dominant query shape: 0 of 9 embedding models beat BM25 alone when queries reuse the transcript's wording (text-only MRR 0.223 / R@5 37.2 pct vs best hybrid 0.249). BM25 is not the weak half — CAP-9 must fund full-text quality (domain synonyms, field boosts), not treat it as a fallback behind the vector store
- (note) Embeddings earn their place only on paraphrase: best model gains +0.149 MRR over text-only on natural questions (53 pct better), while gaining +0.009 on transcript-worded queries. Hybrid is justified by traffic mix, not by vectors being better in general
- (decision) Recommended embedding model is snowflake-arctic-embed2 (1024 dims) over the in-production embeddinggemma:300m (768 dims): +0.130 MRR on natural questions, the largest effect measured; costs 26.9 vs 49.7 passages/sec, ~5 min to re-index 7730 chunks, +33 pct vector storage
- (note) EmbeddingGemma's officially documented query/document prompts measured WORSE than symmetric raw usage (-0.059 MRR cited claims, -0.046 natural), so documented usage was not shipped — prompting is part of a model, and getting it wrong is invisible without measurement
- (note) Passage boundaries are a larger lever than model choice per the bake-off's own limitations: the strict-vs-adjacent scoring gap says a meaningful share of misses are the answer sitting one chunk over, and chunk size/overlap were held fixed and never varied
- (note) Participant resolution measured across all 28 org charts: 225 person-rows, 50 distinct people, 3 unresolved (1.3 pct), 222 rows carry mail (98.7 pct), 208 carry a managerChain (92.4 pct) up to 5-6 deep
- (decision) CORRECTION to corpus-facts 4: unresolved attendees are NOT flagged by the guest field — guest is false on all 225 rows corpus-wide. The actual marker is unresolved:true plus org:'Unknown' (Taylor Brooks, Hayes Morgan, Riley Parker). A builder keying on guest finds nothing; key on unresolved
- (decision) CORRECTION to the epics story-1.5 rationale: the premise 'Graph is a non-goal, so no directory identifier exists' is falsified. The org chart carries a real mail address on 222/225 rows, sourced from the SharePoint user-profile service over the existing cookie session — NOT from Microsoft Graph. The employee-number login (10001@corp.com) is a separate field; mail is the stable join key and it exists without Graph
- (note) Graph non-goal itself survives intact: orgSource is 'sharepoint', Graph is supported but unconfigured (needs GRAPH_CLIENT_ID + app registration). Enabling Graph would only add calendar invitee accept/decline status — the invite/response fields are present but empty today
- (note) Attendee provenance is dual-sourced per occurrence — attendeeSources records e.g. 'recording permissions (6)' and 'transcript speakers (11)', and per-person foundIn records which produced each row; recording permissions surface non-speaking attendees the transcript alone cannot
- (note) All 28 org charts carry meeting.streamUrl, and every _source.json carries the same URL as its url field — the transcript-only deep-link affordance has a verified source on 100 pct of occurrences, not a hoped-for one
- (event) Self-validate pass 1 (coherence) after live-copy rollup: PASS — IDs stable CAP-1..9, none added or renumbered; the new full-text constraint bends a real decision (it reverses the default of treating BM25 as fallback and funds synonyms/field boosts under CAP-9); the Graph non-goal stays a non-goal but now states the true participant source; kernel stays WHAT, all measurements pushed to companions
- (event) Self-validate pass 2 (preservation) vs the live pull_transcript copy: PASS — bake-off method/findings/model recommendation/limitations in retrieval-prior-art.md 7, store contents and chunking in 6, corpus and participant measurements in corpus-facts.md 1 and 4. Deliberate drops (their-track-only, logged not silent): Kuzu DDL and extension mechanics, Ollama host specifics, launchd/cron scheduling, migrate-layout history, the org-chart CLI flags, and the 11-occurrence summary backfill list
- (event) Spec re-rendered 2026-08-18 (live-copy rollup): SPEC.md +1 constraint, Graph non-goal and drop-contract assumption corrected; corpus-facts.md inventory + participant resolution rates + guest/mail corrections; retrieval-prior-art.md sections 6-7 added and the contents-unknown boundary in 5 resolved
- (note) Downstream impact flagged, not yet applied: epics.md story 1.5 carries the falsified 'no directory identifier exists' rationale and should gain a mail-keyed participant identity AC; story 1.7 should gain the fund-full-text constraint
- (direction) User (2026-08-18): recovering video for the transcript-only backlog is active work in progress inside the organisation — the transcript-only majority is a dated snapshot moving in the recovery direction, not a settled property of the corpus
- (direction) User (2026-08-18): the mechanism behind the video gap is that a Teams recording lands in the personal OneDrive of whoever hit record, so recovery depends on each recording's owner acting
- (note) Recording ownership measured from Stream URL paths across 28 occurrences: 5 distinct owning OneDrives, concentrated — one owner holds 16 of 28 occurrences with only 5 local mp4s. Both large owners hold a mix of downloadable and non-downloadable items, corroborating the existing per-recording-not-per-owner finding
- (constraint) Do not justify the transcript-only path by the current transcript-only ratio: it is first-class because some meetings will never have retrievable video (retention expiry, per-recording permissions, externally-hosted sessions), not because most currently lack it. Tying the justification to the count deprioritises the path exactly as the count falls
- (constraint) Size the video path for the recovered corpus, not today's 8 recordings: capture pipeline, over-capture guardrail, and storage assume most of the 169 transcript sources eventually acquire ~120-minute recordings
- (question) CAP-1 has no path for a meeting acquiring video AFTER it was ingested transcript-only, which the active recovery work makes routine rather than an edge case. The drop contract is write-once (a re-pull never overwrites a finalized drop, AD-1) and transcript-only ingests SKIP the probe/frames/ocr/screens/transcribe stages, while sourceId for a transcript-only occurrence is its Stream URL — the same value a later video-bearing re-pull would carry. Unresolved: does late video augment the existing meeting in place (run the skipped stages, preserve existing moments, citations and published artifacts), supersede it as a new drop version (rebuilding moments and orphaning citations), or stay out of capstone scope
- (decision) User (2026-08-18) resolved the late-video question: AUGMENT IN PLACE. A recording arriving after a transcript-only ingest is recognised as the same meeting and runs only the stages that were skipped, adding screens, screenshots, alignment and replay while preserving the existing moments, their citations, and any already-published artifacts
- (constraint) Augmentation adds, never destroys: a later drop may attach a screenshot, replay window and alignment to an existing moment and may add new screen-derived moments, but must never delete, renumber or re-key a moment that already exists. Existing citations and published artifacts stay valid across augmentation, because a citation that breaks when better evidence arrives violates no-citation-no-answer at exactly the moment the evidence improved
- (constraint) Write-once applies to a drop, not to a meeting: a finalized drop is still never overwritten, but one meeting may accumulate more than one drop over time. The drop contract must therefore let a later drop declare which meeting it augments rather than colliding on sourceId — the exact schema is an architecture decision (AD-1)
- (decision) User (2026-08-18) resolved the transcript-only replay question: SOURCE DEEP LINK, EXPLICITLY TRANSITIONAL. Transcript-only moments carry an open-source-recap link built from the Stream URL (verified present on all 28 occurrences), specified as the affordance that holds until real video lands and is replaced by true replay when augmentation delivers it
- (note) The two 2026-08-18 decisions compose: the deep link is the transitional affordance and augment-in-place is the mechanism that retires it — so the link is specified with its own end condition rather than as a permanent parallel feature
- (event) Self-validate pass 1 (coherence) after the late-video and deep-link decisions: PASS — CAP-1..9 stable, none added; both new constraints bend decisions (augmentation-adds-never-destroys rules out rebuild-on-re-ingest and forces stable moment identity; drop-vs-meeting write-once rules out sourceId collision as the drop key); CAP-1 first-classness no longer rests on the transcript-only ratio; Open Questions now empty and every prior entry traced to where it landed
- (event) Self-validate pass 2 (preservation) across the four statements of the transcript-only rule: PASS — SPEC.md CAP-1/CAP-4, ux-spine.md, architecture-diagrams.md and scope.md all restated consistently (deep link present, augmentation reversible, ratio dated). Known remaining divergence is downstream and outside this skill's writer scope: epics.md UX-DR11 and story 1.6 still say no replay link is produced
- (note) Downstream impact now three items, all epics.md and outside bmad-spec's writer scope: UX-DR11 + story 1.6 contradict the deep-link decision; no story covers the augmentation path; story 1.5 still carries the falsified no-directory-identifier rationale and story 1.7 lacks the fund-full-text constraint
- (decision) epics.md reconciled to the amended SPEC (2026-08-18, targeted edits not a regenerate, to preserve story numbering and the in-flight state of 1.4/1.5/1.11): new FR32 augmentation; FR3 no longer rejects an augmenting drop on sourceId; FR4 records the transcript-only stage skip as reversible; FR16 and UX-DR11 and both epic-2 restatements carry the transitional deep link
- (decision) epics.md story 1.5 corrected: identity keys on the participant graph mail field (222/225 rows, SharePoint user-profile service, not Graph) with name normalization as fallback; added that unresolved externals are detected by unresolved:true and org Unknown, never by the guest field which is false corpus-wide
- (decision) epics.md story 1.6 AC changed from 'no replay link is produced' to a transitional source deep link from the drop Stream URL, retired when augmentation supplies video; story 1.7 gained the fund-full-text AC (BM25 beat every embedding model on transcript-worded queries) and a chunk-size-and-overlap-as-recorded-tuning-lever AC
- (decision) New epics.md story 1.12 Late-Recording Augmentation added to epic 1 and registered in sprint-status.yaml as backlog: augmenting drop accepted, only skipped stages re-run, pre-existing moment identity preserved, prior citations and published artifacts still resolve, deep link retired, meeting re-projected by id
- (note) Incidental fix while editing epics.md: the Epic List entry for Epic 2 used a level-2 heading while epics 1/3/4/5 used level-3, so it collided with the real Epic 2 section heading; normalised to level 3
- (note) Build state 2026-08-19 (user): one builder, working story 1.12 Late-Recording Augmentation — the last open epic-1 story; 1.1-1.11 are done in sprint-status.yaml
- (question) The augment-in-place decision is stated in SPEC (CAP-1, the augmentation-adds-never-destroys and drop-vs-meeting constraints) and in epics FR32/FR3/FR4/story 1.12, but the mechanism it delegates to was never defined: docs/source-drop.schema.json carries no field by which a drop declares the meeting it augments (properties are schemaVersion, sourceId, corpus, startedAt, startedAtPrecision, provenance, participants), ARCHITECTURE-SPINE AD-1 never mentions augmentation, and its intake rule still reads 'a POST /ingests whose sourceId already has a non-failed job is rejected with an RFC 9457 conflict'. Implemented behaviour matches the spine, not the spec: server/meetingminer/api/ingests.py returns 409 and server/tests/test_ingests.py locks it in
- (note) SPEC Open Questions stays empty: the spec resolved the WHAT and explicitly delegates the drop schema to spine AD-1, so this is a downstream-impact item (bmad-architecture owns ARCHITECTURE-SPINE.md and the drop schema), not a spec-level gap. Left as a flag for the 1.12 builder rather than decided here
- (decision) The augmenting-drop mechanism question logged earlier is ANSWERED — by the 1.12 builder, on branch story/1-12 (13 commits, story marked done, not merged to main as of 2026-08-19). Drop schema goes to schemaVersion 2 with an optional augments object requiring augments.sourceId, deliberately allowed to differ from the drop's own sourceId so a recording recovered from a personal drive keeps its own drive-item id; augments implies version 2 so a v1-pinned consumer fails closed rather than ingesting a second meeting. Intake does NOT open a second job: it re-arms the declared occurrence's existing job in place against the new drop, re-queuing the video stages plus align and moments and answering 200 with the existing job id, so meeting.job_id/meeting.source_id stay UNIQUE and the meeting id — and every moment id, citation and published artifact naming it — survives. The builder amended ARCHITECTURE-SPINE AD-1 and AD-14 on that branch to match
- (note) Two-writer hazard flagged to the user 2026-08-19: a separate bmad-architecture session was started on main to decide this same AD while story/1-12 already carries an AD-1 + AD-14 amendment and passing tests for it (test_augmentation.py, test_ingests.py intake matrix, test_drop_schema.py). Same file, two writers — merge conflict at best, an AD contradicting shipped code at worst
- (note) SPEC re-derive deferred until story/1-12 merges: the drop-vs-meeting constraint's clause 'the exact schema is an architecture decision (spine AD-1)' can then cite the concrete augments declaration instead of forward-referencing an undecided AD. No kernel field changes either way
- (event) Two-writer hazard resolved without a conflict 2026-08-19: the architecture session ABORTED before editing, reverted its drafted AD-1/AD-4/AD-5/AD-11/AD-13/AD-14 amendment uncommitted, and repointed itself to review-and-ratify. Main's ARCHITECTURE-SPINE.md is untouched (commits 1b8e92d and ec1d9de append to the architecture memlog only); the spine amendment stays on story/1-12 until it merges. Ratification verdict: SOUND, and better than the architect's own draft on schemaVersion fail-closed and on re-arming the existing job instead of opening a second one
- (note) Architect gap 1 is the mechanism the SPEC constraint 'augmentation adds, never destroys' rests on, and it is unstated anywhere: moment identity survives augmentation only because transcript_segment.start_ms keeps the PROVIDED transcript's cue timing while the STT lane writes stt_start_ms and alignment_delta_ms in separate nullable columns (migration 0005), so identity_key 'transcript:<start_ms>' cannot move when a recording lands. It stays OUT of SPEC.md by Spec Law 2 (kernel states WHAT, not HOW) and belongs in spine AD-13 or AD-6 — but if a future capture or segmentation retune breaks it, every pre-existing citation breaks with it and no spec-level assertion would catch it
- (note) Architect gap 4 (pre-existing, unrelated to 1.12) closes a loop this spec opened on 2026-08-18: spine AD-5 still says participant dedup keys on AAD object ID, while SPEC's Graph non-goal, the drop schema, and pipeline/speakers.py (MAIL_NAMESPACE) all key on the source-supplied mail address. The spine is now the last artifact carrying the falsified no-directory-identifier premise — epics story 1.5 was corrected 2026-08-18
- (note) story/1-12 is with the codex reviewer at cdd8191, still unmerged. Its reviewer handoff prompt (review-prompt-story-1-12-2026-08-19.md) predates the architect's 12:33/12:35 gap list and does not carry any of it — the reviewer will not see the identity-invariant or recovery-convention findings unless handed them. SPEC re-derive stays deferred until the merge
- (event) story/1-12 merged to main 2026-08-19 (aeeb309 story + retry-preservation fix, f7f5ac1 baseline repair); make test green (puller 74/74, web 38/38, server suite); both worktrees removed. The spine amendment to AD-1/AD-14 and drop schemaVersion 2 are now on main, so the SPEC re-derive deferred earlier is unblocked
- (constraint) Merge-time generalization of augmentation-adds-never-destroys, discovered by the reviewer and shipped in b8b7b37/7ea0085: the no-rewrite rule binds ANY later drop pointed at an existing meeting, not only a declared augmentation. Retrying a failed job that already minted a meeting must keep that meeting's corpus, wall-clock instant and precision, must still carry every provided transcript the current drop carries, and may not return a meeting that already has a recording to transcript-only. A failed ingest that never minted a meeting keeps its unrestricted re-queue path. Load-bearing because the failed-retry route was the one path that could destroy a cited meeting without ever declaring augments
- (decision) CORRECTION to the drop-contract assumption: participants are NOT a sibling file in the drop. The shipped contract (docs/source-drop.schema.json, spine AD-1) makes participants an OPTIONAL embedded array in metadata.json; the puller's org chart.json stays in its native output and is never copied into a drop. corpus-facts 4 describes the puller's upstream file, not the drop — the assumption conflated the two layers
- (question) OPENED (self-validate pass 1, 2026-08-19): participant identity has no input path. SPEC's Graph non-goal claims participants derive from the puller's participant graph carrying a directory mail address, and pipeline/speakers.py identity_key_for keys on MAIL_NAMESPACE when the source supplies mail — but pull_transcript/emit-drop.js deliberately emits no participants key (line 288: story 1.5 derives from transcript speaker attribution), and org chart.json is never copied into a drop. So identity always falls back to normalized display name and the measured 98.7 pct mail coverage never reaches the system. Resolution is a product call — either emit-drop maps org chart.json into the participants array, or the non-goal stops claiming mail-keyed identity. Surfaced, not decided
- (event) SPEC.md re-rendered 2026-08-19 post-merge: drop-vs-meeting constraint now cites the concrete augments.sourceId declaration and AD-1/AD-14 instead of forward-referencing an undecided AD; new constraint added for no-rewrite-on-retry; the drop-contract assumption corrected to the shipped schema (participants optional and embedded, org chart.json not in the drop); one open question added. CAP-1..9 untouched, no capability added or renumbered, companions list unchanged
- (event) Self-validate pass 1 (coherence): PASS — both new constraints bend real decisions (the retry rule forbids a replacement drop that downgrades or re-dates a minted meeting; the augments rule fixes the link as the declaration rather than sourceId). Pass 2 (preservation) vs the merged branch: PASS — the schemaVersion 2 / augments / re-arm-existing-job decision, the retry-preservation fixes, and the corrected participants shape all landed. Deliberate drop (their-track-only): the intake refusal matrix, AUGMENTATION_STAGES membership, and projection-invalidation mechanics stay in the spine and code, not the kernel
- (direction) User (2026-08-19): close the participant-identity gap by making the implementation match the spec, not by weakening the spec's claim
- (decision) The fix is puller-side only — the server half already exists and is unused. pipeline/stages/align.py builds its roster from ctx.drop.metadata['participants'] when present (line 283), keys each person via speakers.identity_key_for(display, mail) at line 313, stores the graph whole as jsonb, and writes meeting_participant with mail/title/department/dept_code. With no participants key in any drop, align always takes the no-graph branch (line 340) and identity_key is the normalized display name for every person in the corpus
- (note) This is a defect against an existing acceptance criterion, not new scope: epics story 1.8 already requires emit-drop to write 'best-effort participants' into metadata.json. pull_transcript/emit-drop.js:280-290 deliberately omits the key, reasoning that 'story 1.5 derives participants from the transcript's speaker attribution, which is better than anything the puller holds'. The 2026-08-18 measurement falsifies that reasoning — org chart.json carries mail on 222/225 rows and a reporting chain on 208, while transcript attribution carries display names only
- (note) Shared root cause worth one fix, not two: the participants gap and FR32's end-to-end reach are both blocked by drop identity. A drop's identity is its directory name <date>-<title-slug>-<sha1(sourceId)[0:8]>, checked with a plain existsSync, so the puller can never emit a SECOND drop for an occurrence it already emitted — it returns status 'exists', emits nothing and POSTs nothing. That is what stops a re-emit carrying participants and what stops an augmenting drop carrying a recovered recording. spec-1-8 already carries drop identity as a deferred design item
- (event) SPEC.md amended 2026-08-19 for the participants decision: new constraint that participants key on the directory mail address with normalized display name as fallback not default; the drop-contract assumption now states the drop carries the mapped participant graph; the open question is closed. CAP-1..9 untouched
- (event) Self-validate pass 1 (coherence): PASS — the new constraint bends a decision (it forbids shipping name-keyed identity as the norm and forces the drop to carry the graph). Pass 2 (preservation): PASS — the measured mail coverage lives in corpus-facts 4, the code-side finding stays in the memlog, and the kernel carries only the rule
- (decision) New epics.md story 1.13 Drops Carry the Participant Graph added to epic 1 and registered in sprint-status.yaml as backlog. No new FR — it delivers the unmet half of story 1.8's existing AC under FR1/FR7, and its re-emit AC also closes FR32 end-to-end, since the same drop-identity 'exists' short-circuit blocks both. Epic 1 therefore stays in-progress rather than done
- (question) Story 1.13 blocker found before the builder hits it (worktree story/1-13 opened at 342519e, no commits yet): the participants backfill has NO intake door. A declared augmentation is refused unless the drop carries recording.mp4 (api/ingests.py:303) and unless the target occurrence has no recording yet; the plain re-queue path applies only when every job for that sourceId has failed (ingests.py:532). So a drop whose only new content is the participant graph is refused for an already-ingested transcript-only meeting AND for one that already has its recording. Resolution is an AD-14 call, not a puller change: widen the augmentation door to accept a drop bringing any evidence the meeting lacks (participants included) rather than a recording specifically, which keeps one intake door; the alternatives are a separate API-side participant import, which breaks AD-14, or a superseding re-ingest, which risks moment identity
- (note) Second 1.13 design constraint, from the architect's post-merge gap 3: reconstruction requires replaying a meeting's drops in emit order, so whatever discriminator the re-emit path adds to the drop directory name <date>-<title-slug>-<sha1(sourceId)[0:8]> must keep emit order recoverable from the drops folder alone. A content hash or random suffix would satisfy write-once and still break recovery
- (decision) epics.md story 1.13 amended to carry the intake-door prerequisite and the emit-order constraint as acceptance criteria. Process correction: when 1.13 was written the puller-side 'exists' short-circuit had been traced but the server-side intake refusals for a recording-less drop had not, so a verified half was extended into an unverified assertion, and the blocker reached the builder as chat rather than as story content. A finding a builder must act on belongs in the artifact the builder reads
- (decision) The viewability-gate deferral becomes acceptance criteria on epics story 2.3 (Meeting Drill-Down), not a story of its own: it is enforcement on a route story 2.3 creates, so it cannot ship independently and a separate story would be forgotten the same way the first deferral was. Added with the augmentation-aware half — the empty state must distinguish an augmenting run from a never-ingested meeting rather than keying on viewable alone
- (decision) The drop-schema metaschema check (Draft202012Validator.check_schema at API startup) stays in deferred-work.md and gets no story: it converts a malformed COMMITTED schema file from a first-intake failure into a named startup failure, on a single-machine capstone whose schema is committed and covered by tests in both the puller and the server. It would not have caught anything story 1.13 changes, since a wrong-but-valid schema passes check_schema. Revisit if the schema starts changing often or a non-editable install becomes real
- (decision) Augmentation generalized in the kernel beyond recordings (1.13 builder's AD-14 widening, plan 11d9cc7): CAP-1 and the augmentation-adds-never-destroys constraint now read as 'a later drop bringing evidence the meeting lacks' — a recovered recording or a participant graph the first drop had not resolved — with the pipeline running only the stages that evidence unlocks and a drop bringing nothing new refused. The recording-specific wording came from story 1.12, which was the only augmentation case that existed then
- (question) Flagged to the 1.13 builder, unresolved by them yet: opt-in per-occurrence --re-emit splits one human across two identity keys. A person in a re-emitted meeting keys on mail:<addr> while the same person in a not-yet-re-emitted meeting keys on name:<normalized>; align/_resolve_participants consults participant_alias by alias_key and nothing writes an alias automatically, so the two participant rows are unlinked and the participants -> meetings -> topics -> moments traversal returns half that person's meetings. That traversal is CAP-2's success criterion and the Rowan demo query, so a half-migrated corpus is worse than a fully name-keyed one because the split is invisible. Recommended: an --all --re-emit pass that reports how many meetings remain name-keyed, rather than a worker-written alias, which would cross AD-5's participant/participant_alias ownership split
- (event) Self-validate after the augmentation generalization: pass 1 (coherence) PASS — CAP-1 stays WHAT (which evidence unlocks which stages, not how intake decides), the constraint still bends a decision, CAP-1..9 unchanged. Pass 2 (preservation) PASS — the widened gate, the align+moments-only re-arm and the projection-invalidation trigger stay in the builder's spine amendment and code, not the kernel
- (decision) Identity-split question RESOLVED by the 1.13 builder: option 1, operational not structural. --all --re-emit covers the 28-occurrence corpus, and emit-drop's summary line now reports how many drop-directory groups (grouped by the <date>-<slug>-<sha1_8> prefix, newest sequence per group) have a newest drop carrying no participants key — so a half-migrated corpus is visible rather than silent, recoverable from the drops folder alone. Covered by a new AC and a Design Note naming the split. Option 2 (worker-written alias) goes to deferred-work.md as the real fix if identity ever migrates incrementally, carrying the constraint that the worker may write an alias only to migrate an identity key forward, never to merge two humans — which would be an AD-5 amendment
- (note) Minor caveat on that report, not raised with the builder because it is moot for this corpus: a drop whose org chart yielded zero usable rows also omits the participants key by design, so it counts as un-migrated. The count is still semantically true (that meeting is name-keyed) but stops being a clean zero-means-done signal if such a chart ever appears. corpus-facts 4 measured all 28 occurrences as carrying an org chart, so today the count does reach zero
- (note) Live pull_transcript copy re-measured 2026-08-19 (user-directed). Video recovery has landed at scale: the archive mirror held 0 .mp4 on 2026-08-18 and holds 77 today, 19.5 GB across 85 .mp4 locally against an indexed 24.7 GB; _index.json generated 2026-08-19T15:00Z reports 45 downloaded / 58 missing of 103, so the fetch was still running while measured. Archive transcripts grew 141 to 193 .vtt (total transcript sources 169 to 221) and 58 .docx design documents appeared, a content type MeetingMiner does not ingest
- (constraint) No silent zero, added to SPEC Constraints: a stage that parses model or tool output into artifacts must report a zero result from input that plainly contains extractable content as a signal, not as success. Evidence is upstream's measured failure — a summary parser understood one of two document layouts, contributed zero decisions for every meeting using the other, reported success, and was found by chance; the fix moved decisions from 41 to 182. Same class as the independently-derived ground-truth rule, applied to extraction (CAP-5) and to eval checks that count what a stage produced (CAP-7)
- (note) New companion section corpus-facts 6 records the upstream VMS_Recordings_Transcripts_Inventory.csv: 269 rows, 2026-02-18 to 2026-07-06, 148 transcripts / 64 recordings / 57 documents; by its own meeting_key, 37 meetings carry both recording and transcript, 21 recording-only, 104 transcript-only. A wider population than the 28 pulled occurrences and the pool the transcript-only backlog is drawn from
- (note) Upstream corroborations needing no spec change: the 3 unresolved participants are vendor vendor staff genuinely absent from the corp people index, blocked on a Graph app registration (GRAPH_CLIENT_ID, delegated User.Read.All + Calendars.Read) — which confirms the Graph non-goal costs only invitee accept/decline plus these externals, and gives story 1.13 its stays-name-keyed-consistently case. Upstream's own next tasks also duplicate two constraints this spec already carries: BM25 synonym tuning (CAP-9 full-text constraint) and chunk size/overlap as the untested lever (story 1.7 AC)
- (event) Self-validate after the 2026-08-19 live-copy re-measure: pass 1 (coherence) PASS — the new no-silent-zero constraint rules out shipping a parser whose empty result is indistinguishable from a quiet meeting, CAP-1..9 unchanged, no capability added. Pass 2 (preservation) PASS — recovery counts and the library inventory in corpus-facts 1 and new 6, the parser failure and superseded store numbers in retrieval-prior-art 6 and new 8, the rule alone in the kernel. Deliberate drops (their-track-only): launchd scheduling, Ollama host specifics, the replay/--login-graph CLI mechanics, and upstream's own task ordering
- (decision) Drop contract sharpened (1.13 builder, commits 11d9cc7/fc61abf/237fe79): an omitted participants array and an empty one are different statements — omitted means the source did not look and transcript attribution fills in, [] means the source looked and found nobody and no fallback runs. Symmetric on both sides of the augmentation comparison: a drop carrying [] adds no participants, and a target carrying [] has no graph a later drop would duplicate. Rendered into the SPEC drop-contract assumption because a reader would otherwise take [] as equivalent to omitted
- (note) Second route to the same silent split, found by the 1.13 builder while acting on the partial-migration warning: a re-resolved org chart is a difference but not evidence the occurrence lacks, so intake refuses it — and the puller would already have finalized that drop write-once and POSTed it, read the 409 as 'already ingested', and the next pass would compare against a drop that never ingested and report the occurrence migrated. dropIsCurrent now mirrors the intake door's rule rather than asking 'is anything different', with a test naming the reason. The refusal is 409 augment-adds-nothing, not 400 — the drop is not invalid, the target's state is what refuses it
- (event) story/1-13 green as reported by its builder: make test 742 passed 0 failed (248s, one pre-existing starlette/httpx deprecation warning) plus a clean web build; make puller-test 98 pass 0 fail 0 skipped. Docker stores released
- (note) New compute resource recorded 2026-08-19 (user-provided handoff, health verified live from this machine): VM 120 cuda-asr on the ThreadRipper/Proxmox box — RTX 4080, CUDA 12.9, nvidia/parakeet-tdt-0.6b-v3 over FastAPI at http://10.77.0.120:8000. ~227x real time end to end, native NeMo timestamps rather than estimated, truthful processed_ranges on partial failure, 10-minute chunking on exact sample-frame boundaries. Does not diarize, so it does not repair the speaker-less archive .vtt tracks. Reports language auto with no reliable detected-language label
- (constraint) Anything reached over the LAN is off by default and shared, so a demo-path stage may name one only with a local fallback. VM 120 and VM 116 pass through the same RTX 4080 and must never run at once, and VM 120 has onboot=0 — its availability is a scheduling fact, not a standing guarantee. The existing OCR adapter already models the shape (engine plus fallback, substitution logged once per stage run); the stt block in config.yaml has engine but no fallback key today
- (decision) The local-first assumption is scoped rather than contradicted: it governs evidence and state (content root, database of record, retrieval stores), not model inference. On-prem LAN model hosts were already in use for embeddings (Ollama) before this one, and the SPEC adapter constraint — all model interaction behind adapter interfaces, replaceable by configuration — is exactly what makes a remote STT engine a config change rather than an architecture change
- (question) Downstream impact for the architect, outside this skill's writer scope: ARCHITECTURE-SPINE's deployment paragraph reads 'one environment only: the dev/demo MacBook (M4 Max) ... external egress from MeetingMiner is provider APIs only (Anthropic/OpenAI/OpenRouter)'. That was already inaccurate for the LAN Ollama host and is more so now — a LAN GPU service is neither cloud nor a provider API. The deployment row needs one sentence admitting on-prem LAN model hosts, or the spine and config.yaml disagree
- (note) Cross-boundary AC sweep of epic 1 while both builders were occupied (read-only, no stores). VERIFIED REAL, no gap: story 1.8's POST /ingests exists in emit-drop.js with the full status mapping (201 created / 200 requeued / 409 duplicate) and an --all backfill mode that exits non-zero so a broken backfill cannot look clean; story 1.7's rebuild CLI is a real entry point (meetingminer.projections.cli:main, exposed as uv run rebuild and make rebuild); story 1.5's STT verification lane is genuinely wired — stages/transcribe.py builds the Stt port, calls engine.transcribe, and writes a kind='stt' transcript_source keyed (meeting_id,'stt') so the id survives reruns
- (decision) Story 1.5's participants AC was internally contradictory and that is the likeliest reason the gap survived review: 'Given a drop that omits participants ... Then participants are derived ... joined to the drop's participant graph ... Identity keys on the participant graph's mail field'. A drop that omits participants carries no graph to join or key on, so a reviewer could tick the AC against the no-graph branch and be correct. Clarified in epics.md to name both cases explicitly, with a pointer to 1.13
- (question) Could not verify from this session, and it is a state question rather than a code one: whether the puller's one-time backfill pass has actually BEEN RUN, i.e. whether the ~28 real occurrences exist as finalized drops on disk and have ingested. The epic 1 description carries the backfill as in-scope. The drops location resolves from MM_CONTENT_ROOT in .env, which this session is not permitted to read
- (direction) User (2026-08-19), correcting the LAN-host constraint: VM 120's availability IS guaranteed because they control the Proxmox scheduling — the shared-RTX-4080 exclusivity with VM 116 is a sequencing rule for whoever starts VMs, not an availability risk to design around, and further VMs can be provisioned on request. The demand-a-local-fallback constraint is withdrawn from SPEC and corpus-facts 5; the on-prem GPU host is available infrastructure, not a best-effort dependency
- (decision) CORRECTION to the 2026-08-19 ASR-host note, prompted by the user and verified on the machine rather than from the handoff: the claim 'it does not diarize' was wrong. The installed NeMo carries clustering_diarizer, msdd_models, online_diarizer, sortformer_diar_models and TitaNet label_models. What is absent is deployment, not capability — no diarization weights are cached and the service exposes only /health and /transcribe per its own openapi.json, with 3.6 of 16.4 GiB GPU in use at rest. A diarizer for the Diarizer port (today noop, pyannote documented but not bundled) is a model pull plus an endpoint on that host
- (constraint) Bound on what diarization buys, so the correction does not overshoot: diarization segments speaker TURNS, it does not name them. Its output is anonymous clusters, so for the 193 speaker-less archive .vtt tracks it recovers turn structure but not identity; naming clusters needs voice enrollment or a roster match that does not exist, and 'speaker attribution never guesses' forbids inferring it. A real gain for transcript structure, a partial one for the participant graph and therefore for CAP-2's participants-to-meetings traversal
- (constraint) User (2026-08-19): the live demo runs about three minutes. Rendered into SPEC Constraints because it reorders what must be stage-ready — the budget fits exactly one path end to end (CAP-3 question, cited answer, CAP-4 open the cited moment and replay its evidence), so ingestion, augmentation, extraction, publishing and the eval harness are evidenced by artifacts and the eval run rather than shown, and anything not precomputable before the clock starts is out of the demo. User hedged with 'I believe', so if the real budget differs the script scales but the ordering does not
- (note) scope.md corpus row refreshed to the 2026-08-19 measurement: it still described the 103 archive recordings as reachable but never fetched, which stopped being true — 77 are on disk, 19.5 GB local across 85 .mp4, archive transcripts 141 to 193
- (note) sprint-status.yaml corrected 2026-08-19 to stop advertising in-flight work as unclaimed: 1-13 backlog to review (implementation complete, reviewer findings being remediated on story/1-13), 5-1 backlog to in-progress. The file is the coordination surface between sessions, so a stale backlog entry is how two agents end up on one story
- (decision) glossary.md added as a spec-authored companion and registered first in SPEC.md companions. Definitions were taken from code and the spine rather than paraphrased — moment identity key formats from pipeline/moments.py, participant namespaces mail:/name: from pipeline/speakers.py, STAGE_NAMES / VIDEO_ONLY_STAGES / AUGMENTATION_STAGES / EVIDENCE_STAGES from domain/jobs.py, drop filenames from domain/drops.py, citation wire format from AD-15 — because a glossary that rounds off the mechanism is how two components come to mean different things by one word
- (note) Two term collisions documented in the glossary because both are live in this repo and neither will be renamed: 'artifact' means an extracted ADR/action item AND a planning document under _bmad-output/*-artifacts/; 'spec' means SPEC.md (the kernel contract) AND a story spec file (spec-1-13-*.md). Also disambiguated: participant graph / org chart.json is resolved from the SharePoint user-profile service and is NOT Microsoft Graph, which remains a non-goal — the naming coincidence has already produced one falsified epics rationale
- (decision) User (2026-08-19): Epic 1 is CLOSED FOR DEVELOPMENT. story/1-13 merged to main (its commits e06a5d6, 1e612b6, 1c36a81, 3792ecd are ancestors of main), all thirteen stories are done, and epic-1 flips to done in sprint-status. The Epic 1 UAT runs offline or in parallel rather than as a gate on closing development — a UAT finding therefore comes back as new work against a closed epic, not as a blocker that reopens it. epic-1-retrospective stays optional. Work moves to Epic 2
- (note) Obligations Epic 2 inherits, all already recorded and easy to lose at an epic boundary: story 2.3 carries the two viewability-gate ACs added 2026-08-19 (server-side refusal of an unsettled meeting, and an empty state that distinguishes an augmenting run from a never-ingested meeting rather than keying on viewable alone, because align deletes transcript segments before moments re-runs); story 2.4 participant curation now sits on top of 1.13's mail-first identity keying and the deferred worker-written-alias option, which would be an AD-5 amendment because the API owns participant_alias; and the three-minute demo budget makes Epic 2 plus Epic 3 the only path the audience sees
- (event) Story 5.1 (ground-truth schema and scripted fixtures) merged to main at 3f63bdf with all four review patches applied; sprint-status 5-1 done, epic-5 corrected from backlog to in-progress since its first story has landed. evals/ground-truth.schema.json, the validating loader, subject selection and two scripted fixtures are now on main — the first machine-checkable expectations this project has
- (direction) User (2026-08-19), on the story 2.1 builder's report: media storage is not specified and that is a real deficiency — where a brought or downloaded video lands, where each derived artifact lands, and what the database records about the process as provenance must all be defined. Asked for the mess cleaned up, the filed story reviewed, and any further stories written so every builder reads one answer
- (decision) Root cause of the story 2.1 dispute, verified in the tree rather than from the report: the architecture uses TWO path anchors and states that nowhere in the contract. Migration 0005 comments carry it — transcript_source.drop_relative_path for material that ARRIVED (relative to the drop directory) and transcript_source.content_path for material this pipeline PRODUCED (relative to MM_CONTENT_ROOT), each beside sha256 and byte_size. AD-3's single sentence ('video, extracted frames and screenshots live under one content root') reads as one anchor, so a reviewer comparing the recording to AD-3 correctly reports a violation that is really an undocumented rule
- (decision) The real defect is narrower and worse than 'the video is in the wrong place': job.drop_path is an absolute path (0001_jobs.sql, intake requires absolute at api/ingests.py:131) and it is the anchor for EVERY arriving file, not just the recording. Provided transcripts store no segments and are re-parsed from drop_relative_path on every stage run (0005 comment); the augmentation door re-reads an ingested occurrence's metadata.json (_target_drop_has_participant_graph, ingests.py:312); story 2.1's replay route composes job.drop_path plus the RECORDING_FILENAME constant. Moving the drops folder therefore breaks replay, transcript re-parse and augmentation together
- (decision) Chose the configured-anchor option (the other session's option 2) and REJECTED copying the recording under the content root (its option 3, the filed story). Copying fixes replay only and leaves transcript re-parse and the augmentation door still resolving through an absolute job.drop_path, so it buys a third of the relocation guarantee for a multi-GB duplicate per meeting (measured corpus: 19.5 GB across 85 .mp4) plus a new worker stage, crash-safety and disk-full handling. AD-1 already makes the drop permanent — never renamed, rewritten or deleted — so a copy is a second permanent copy of a permanent file. The copy-versus-hard-link trade the filed story escalated dissolves: there is no copy
- (constraint) Two anchors, both configured, both permanent. Arriving material (recording, provided transcripts, metadata.json) stays in its write-once drop and is recorded relative to the DROPS ROOT; derived material (frames, screenshots, extracted audio, artifacts) is written under the CONTENT ROOT and recorded relative to it. Neither root's absolute location is ever stored in the database or leaves the server. Both are permanent storage that must be backed up together: the drops root is not a landing zone that can be cleared, because ingested drops are re-read for transcript re-parse and for the augmentation comparison long after ingest
- (constraint) Every evidence file has a database row that names it, and no path is half data and half code. The row carries which root it is anchored to, its path relative to that root, its sha256 and its byte size, and the stage that produced or read it. Composing a served path from a stored value plus a hardcoded filename constant is the defect this rules out — story 2.1 served the recording as job.drop_path plus the domain/drops.py RECORDING_FILENAME constant, which is why a swapped recording is undetectable where a swapped transcript is not
- (decision) storage-layout.md added as a spec-authored companion, registered in SPEC.md companions after glossary.md. It is the single answer to 'how are we storing videos': the two roots and their configuration, the anatomy of a drop, the content-root tree, the anchor rule per file class, the provenance columns each row carries, and the bring-your-own-recording path. Written because the answer existed only as migration comments, one spine sentence and per-story invention, which is exactly how two builders came to disagree about where a video lives
- (question) Bring-your-own recording is named in AD-1 ('Every source — Teams puller, local recording, future YouTube — lands as a source drop') and is unimplemented and undocumented: no tool mints a drop from a local video, and docs/ holds only source-drop.schema.json, README.md and agent-kickoff-prompt.md. A user with a downloaded video today has to hand-author metadata.json against the schema and POST an absolute path. Filed as a story rather than left as a gap, because the user raised it as the first thing that should be clearly defined
- (decision) Two stories written on main and registered in sprint-status: 2-1a-evidence-paths-anchored-to-configured-roots (MM_DROPS_ROOT config, job.drop_relative_path, meeting_media drop_relative_path + sha256, resolve-at-use-time in runner/intake/augmentation door/media route, backfill for the existing absolute rows that exits non-zero on any row it cannot place) and 2-1b-bring-your-own-recording-drops (a command minting a schema-valid drop from a local video, content-hash sourceId, atomic finalize, documented in docs/README.md). 2-1a carries a warning that it SUPERSEDES spec-2-1-recording-under-the-content-root.md, which exists only on branch story/2-1 and must be deleted when that branch merges
- (question) For the architect, outside this skill's writer scope and now the second item queued against the spine: AD-3 reads 'Video, extracted frames, and screenshots live under one content root (MM_CONTENT_ROOT); databases store paths relative to that root only.' That is one anchor where the code has always had two, and reading it literally is what produced the withdrawn copy-the-video story. AD-3 needs one sentence admitting the drops root as the anchor for arriving material, pointing at storage-layout.md. Queued alongside the existing deployment-paragraph amendment about on-prem LAN model hosts
- (note) deferred-work.md exists only on branch story/2-1, so its spec-2-1 entry — which records the resolution as 'the filed story conforms the code rather than amending the contract' — is now wrong and cannot be corrected from main without creating a merge conflict. It needs fixing on that branch or at merge: the contract was underspecified rather than violated, and the conforming story is 2-1a
- (event) Self-validate after the storage-layout derive: pass 1 (coherence) PASS — the two new constraints bend real decisions (they rule out copying the recording, rule out a clearable drops folder, and rule out a served path composed from a stored value plus a code constant), CAP-1..9 unchanged, no capability added, no non-goal touched. Pass 2 (preservation) PASS — the two-anchor rule, the drops-root permanence argument, the per-file provenance row shape and the bring-your-own-recording path all land in storage-layout.md with the kernel carrying the two rules alone. Deliberate drops: the copy-versus-hard-link analysis (moot once nothing is copied) and the per-story code line references (they belong in the story specs, not the contract)
- (decision) CORRECTION to this session's own report: both spine amendments queued at memlog 198 (deployment paragraph vs on-prem LAN model hosts) and 222 (AD-3 as a single content root) are ALREADY APPLIED and committed on main — 01c8dfd 'admit the drops root, scope local-first to state' and ecdbb61 'close the reviewer-gate findings on the storage amendment'. AD-3 is retitled 'relative to one of two roots', names MM_DROPS_ROOT and MM_CONTENT_ROOT, states the arrived-vs-produced anchor rule, rules out copying the recording, and points at storage-layout.md; the deployment paragraph carries VM 120 cuda-asr and the two egress directions. Both questions are closed. They were reported as pending because the memlog was read without checking the spine — the memlog records what was queued, never what the queue's owner then did, so a question against another skill's artifact must be re-verified against that artifact before it is repeated
- (note) Same re-verification pass closed three more questions the memlog still carried open: q90 (two SPEC/spine lineages sharing filenames) was answered by user direction at memlog 96 — this repo's CAP-1..9 and AD-1..16 are canonical, the spike cluster is a parallel track; q167 (participant identity had no input path) is recorded resolved in SPEC Open Questions and landed as story 1.13; q177 (the participants backfill had no intake door) was resolved by the AD-14 widening — api/ingests.py now admits a drop that 'adds a recording the meeting has not got, or a participant graph', not a recording specifically. q183 (--re-emit splitting one human across mail: and name: keys) is not closed but is tracked as an obligation on story 2.4 rather than as a spec question
- (question) The one question that survives re-verification, unchanged and still unanswerable from a session that cannot read .env: whether the puller's one-time backfill emit pass has actually BEEN RUN — whether the ~28 real occurrences exist as finalized drops on disk and have ingested. It is a state question, not a contract question, so it stays out of SPEC Open Questions, but Epic 1 was closed for development with the backfill in its scope, so nothing else will ask it
- (constraint) The two-storage-roots constraint states a target the code has not reached, and SPEC stated it as if current. AD-3 carries the caveat explicitly ('Not yet true in code: MM_DROPS_ROOT does not exist, job.drop_path is stored absolute, transcript_source.drop_relative_path holds a bare filename — story 2.1a closes all three'); SPEC did not, so a builder reading the kernel alone would assume MM_DROPS_ROOT is configured today. Kernel constraint amended to name story 2.1a as the closer. The rule itself is unchanged and still binding
- (event) Self-validate after the re-verification derive. Pass 1 (coherence) PASS — CAP-1..9 untouched, no capability added or renumbered, non-goals and success signal unchanged; the only kernel edits are the story-2.1a caveat appended to the two-storage-roots constraint (the rule itself unchanged and still binding) and four closures appended to Open Questions. Pass 2 (preservation) PASS — every claim rendered was verified in the tree this session rather than carried from the memlog: AD-3's retitle and MM_DROPS_ROOT wording and the deployment paragraph read at ARCHITECTURE-SPINE.md lines 183 and 315, their commits confirmed as ancestors of main, the widened augmentation door confirmed at api/ingests.py, and storage-layout.md already carrying the same 2.1a caveat for transcript_source. Deliberate non-render: the backfill-has-it-run question stays memlog-only because it is state rather than contract
- (decision) Story 5.2 (deterministic capture checks, eval-design 2.1-2.4 + immutable run artifacts) written as spec-5-2-deterministic-capture-checks.md and set ready-for-dev. Picked as the next builder story because it was the only unblocked backlog item at the time and touches evals/ alone, so it cannot collide with the in-flight story 2.1 review under server/
- (decision) Three algorithm decisions the story pins that eval-design left open, each recorded as an additive note back to eval-design: the haystack for check 2.1 is the pipeline's stored frame_ocr text for a screenshot's representative frame rather than a harness re-OCR (the independence rule binds the DENOMINATOR, and both failure directions of a shared OCR engine fail the run, which is the safe direction); 'fuzzy token-set match >= 0.8' is defined concretely as difflib per-token presence at 0.85 over normalize_anchor-folded text rather than by adding rapidfuzz, whose undocumented semantics would silently become the contract; and 'distinct captures' in 2.2 means screenshot rows
- (constraint) Story 5.2 must keep make evals-test store-free: AGENTS.md now advertises it as safe to run concurrently, so the store-backed checks get their own make evals-run target. The harness also connects with default_transaction_read_only=on rather than by convention, because AD-16's publish-gate guarantee (story 5.3) is void if the harness can write what it audits
- (note) Story 5.2 lands with its live run RED by design and the spec says so in three places: both ground-truth fixtures still carry placeholder source_ids, so select_subjects returns zero subjects and the first check fails naming both. A green evals-run today would mean the selector or the zero-subject failure was weakened - the no-silent-zero constraint applied to the harness itself
- (decision) Story 2.1 (media streaming and replay foundation) is DONE, flipped from in-progress in sprint-status. Verified in the tree rather than from a report: api/media.py and its 72 tests are on main (18e8ae1, 7bbbd36), branch story/2-1-review-r3 is an ancestor of main with zero commits and an empty diff, and review-story-2-1 plus the builder handoff record the pass. The reviewer deliberately left tracking at in-progress to keep the conformance work visible; that is what story 2.1a is for, and an in-progress entry nobody is working on is the same coordination failure as a backlog entry somebody is
- (decision) Story 2.1a set ready-for-dev and three stale claims in its spec corrected against the tree: its warning said media.py exists only on story/2-1 (it is on main, so the story is unblocked and starts from main); its Code Map annotated media.py and test_api_media.py as branch-only; and it named migration 0009 when 0007 is the highest applied, so the next free number is 0008. The withdrawn copy-the-recording spec is confirmed absent from main and survives only on the stale story/2-1 branch, which should be deleted rather than merged
- (note) Two builders can now run in parallel - 2.1a under server/ and 5.2 under evals/ - with no file overlap, but both are store-backed and worktrees do not isolate the Docker stores, so they must not run their suites at the same time (AGENTS.md). 2.1a is the higher priority of the two: 2.1b sits behind it and every later Epic 2 story inherits the two-root path contract
- (event) No SPEC.md derive this session. Everything decided was story-level or tracking: CAP-1..9, constraints, non-goals, success signal, assumptions and open questions are all unchanged, and the three algorithm decisions belong in eval-design.md as an additive note the 5.2 builder writes when the code lands, matching how story 5.1 amended eval-design section 1. Pass 1 and pass 2 therefore reduce to confirming the kernel was not touched
- (event) Story 2.1a claimed by a builder 2026-08-19 and flipped ready-for-dev to in-progress in sprint-status, so the coordination surface stops advertising it as unclaimed. It holds the shared Docker stores while it runs its store-backed suite; 5.2 stays ready-for-dev and a second builder can start it in its own worktree as long as the two never run store-backed tests at the same time
- (event) Two builders running in parallel from 2026-08-19: 2.1a under server/ and 5.2 under evals/, both flipped to in-progress in sprint-status. Zero file overlap; the only shared resources are the Docker stores (one agent at a time) and sprint-status.yaml itself, on different lines. Epic 2 and Epic 5 are both in-progress
- (event) Story 5.2 (deterministic capture checks + immutable run artifacts) is DONE and merged to main, verified in the tree rather than from the report: story/5-2 and story/5-2-review are both ancestors of main, main and origin/main are both at 9aa81d6, and sprint-status carries 5-2-deterministic-capture-checks-with-immutable-run-artifacts: done. Epic 5 stays in-progress; 5.3, 5.4 and 5.5 are backlog. Story 2.1a remains the only other in-progress story
- (decision) The 5.2 builder's additive note to eval-design.md landed only two of the three algorithm decisions the story pinned. Section 2.4a records the difflib threshold and the meaning of 'distinct captures', and evals/README.md carries the third with its full argument, but the frozen contract does not: eval-design 2.1 still reads 'OCR every captured PNG' while evals/harness/corpus.py LEFT JOINs frame_ocr on screenshot.representative_frame_id and never re-OCRs anything. A contract that describes an OCR pass the harness does not run is the same defect class as a served path composed half from data and half from code — the reader who has only the contract is wrong about what the check measures
- (event) Self-validate after the eval-design 2.4a amendment. Pass 1 (coherence) PASS — CAP-1..9, constraints, non-goals, success signal, assumptions and open questions all untouched; no capability added or renumbered. The kernel needed no edit because its independence constraint already binds the denominator in so many words ('a self-derived DENOMINATOR reports 100% while measuring nothing'), so the harness reading the pipeline's own frame_ocr does not contradict it. Pass 2 (preservation) PASS — the third algorithm decision and its independence argument now land in the contract companion where the other two already were, verified against the code this session (evals/harness/corpus.py CAPTURE_COLUMNS and the LEFT JOIN frame_ocr, evals/harness/checks.py ocr_defects) rather than carried from the memlog. The 2.4a preamble was corrected at the same time: it claimed 2.1-2.4 stood unchanged, which was false for 2.1's algorithm line, and now scopes the unchanged claim to 2.2-2.4 plus 2.1's formula, threshold and independence rule. Deliberate non-render: nothing added to SPEC.md, and evals/README.md left alone since it already carried the argument
- (note) Correction to memlog 229: deferred-work.md is on main and always was, not only on branch story/2-1. Main's copy already carried the correct spec-2-1 resolution (follow the drop-relative convention, do not copy the recording); only the story/2-1 copy holds the stale text, and it dies with that branch. The earlier claim came from reading the branch without diffing it against main
- (event) Backfill question answered: the participant-graph re-emit pass has not been run. Verified 2026-08-19 on disk and in the store. All 28 finalized drops are schema version 1 with the participants array omitted and no augments declaration. Emit and ingest are complete: 28 meetings, 28 jobs, 8 with a recording, 20 transcript-only. All 51 participant rows are name-keyed, none mail-keyed. Nothing is broken today because a uniform old-contract corpus splits nobody, but identity rests on the key SPEC calls the fallback: no directory mail, no reporting chain, same-named humans collapse. Recorded in deferred-work.md under the story 1.13 entry, not in SPEC, because it is state rather than contract
- (event) Self-validate after the deferred-work cleanup. Pass 1 coherence PASS: no kernel edit, CAP-1 through CAP-9, constraints, non-goals, success signal, assumptions and open questions all unchanged. The backfill finding is corpus state, and the constraint it bears on (participants key on the directory mail address, normalized name as fallback) is a design rule that stays binding whether or not the corpus has migrated. Pass 2 preservation PASS: both load-bearing findings landed in deferred-work.md, which is where a builder will meet them, and neither is decoration. Branch story/2-1 could not be deleted from this session because the permission classifier blocked the command, so that item is handed to the user rather than reported done
- (note) User named a live puller working copy at /Volumes/nvmepool/mm_current/pull_transcript, rsynced periodically from the machine running the real pulls and refreshed twice on 2026-08-19 (its archive index carries generatedAt 2026-08-19T15:00:18Z). It is not this repo's vendored pull_transcript and must be treated read-only: another process writes it. Surveyed 2026-08-19, joining on provenance.url, which drop metadata copies verbatim from the occurrence _source.json while sourceId holds the canonicalized form of the same URL
- (note) Survey result. The volume holds 29 pulled occurrences against this corpus 28 drops, joining 28 of 28 with one occurrence never dropped (project- User Roles Review, 8.13.26, transcript only). All 28 joined occurrences carry an org chart.json, so the participant-graph backfill input is completely present and the re-emit pass is unblocked. Separately the volume carries a Recordings and Transcripts tree, 77 mp4 of the 85 total, pulled from the VMS team SharePoint site rather than the personal OneDrive recaps, indexed by an _index.json regenerated today. Matching it against the 20 transcript-only drops by recording timestamp and then by exact filename recovers a recording for 9 of them, leaving 11 with none, among them all three Boomi standups. The two NDA demo recordings that corpus-facts names the primary capture-eval assets are in that tree under Functional Demo Transcripts - R2C, dated 6.23.2026
- (event) The participant backfill has now been RUN, 2026-08-19, and the corpus is on the mail key. Sequence: rsynced the 29 org chart.json files plus two new occurrences from the NAS working copy into this repo pull_transcript, which is where emit-drop.js scans since --all resolves to its own __dirname; ran emit-drop.js --all --re-emit, which wrote 28 augmenting sibling drops at -002 carrying schemaVersion 2 and augments plus one brand-new drop for the 8.13.26 project User Roles Review occurrence, with zero unusable charts. Result after align and moments re-ran: 29 meetings, participants linked to a meeting are 47 mail-keyed and 3 name-keyed, the three being speakers the org chart could not resolve, which is the designed fallback. 48 old name-keyed rows are now orphaned with no meeting link. The 8 recordings were never re-sampled because a participants-only augmentation re-queues align and moments alone
- (constraint) Latent defect found by running the pass: the api loads docs/source-drop.schema.json once at startup and caches it, and uvicorn --reload watches .py files only, so a schema change never reaches a running api. The api had been up since 08:07 while story 1.13 committed the v1-to-v2 schema at 14:42, so every one of the 28 augmenting drops was refused 422 invalid-drop, augments was unexpected, schemaVersion 1 was expected, against a schema file that had supported version 2 for hours. Restarting the api fixed it and all 28 were then accepted. The drops themselves were already written and write-once, so nothing was lost, but the failure reads as a bad drop rather than a stale process. Worth a startup log line naming the loaded schema version, and it compounds the existing deferred item about docs_root anchoring the schema to the config file's parent directory
- (decision) User asked whether to alias the 48 orphaned name-keyed participant rows and whether the follow-ups should become stories. Yes to both, and the aliasing is safer than it first looked: align resolve_participants consults participant_alias first and unconditionally on the roster entry's identity key, so a name-keyed alias is only ever reached when the graph supplied no mail for that speaker. It therefore cannot degrade the good path, and the same-name collapse the mail key exists to prevent does not apply. Recording an alias is the table's designed meaning, a merge a human performed, and without one a legacy drop re-ingested without a graph re-mints all 48. Measured: 46 of 48 map 1:1 by normalized name with no contention, 2 are mononym display names with no directory counterpart and stay unaliased. Not run by hand because AD-5 assigns participant_alias writes to the API and no such path exists; building it is story 2.4, and the table holds zero rows today so this is its first use
- (event) Three follow-ups registered rather than left in chat: the alias backfill folded into the existing backlog story 2-4-participant-curation, a new story 2-6-source-drop-schema-reloaded-on-change for the startup-cached schema defect, and the recordings import recorded against 2-1b per user decision to wait for it. Each has a deferred-work.md entry carrying the measured evidence and a sprint-status comment pointing at it. Self-validate: pass 1 coherence PASS, no kernel edit, CAP-1 through CAP-9 and every constraint, non-goal and success signal unchanged, since all three are implementation follow-ups rather than contract changes. Pass 2 preservation PASS, every claim rendered was measured in the store or read in the code this session
- (direction) Standing user rule for story dispatch, 2026-08-19: never recommend stories to run in parallel unless they are completely caveat-free and completely independent. A recommendation that comes with conditions - coordinate the store-backed suites, the files are largely disjoint, just do not run the tests at the same time - is not a parallel recommendation and must be given as a sequence instead. The user would rather run stories one at a time than manage caveats. This supersedes the pattern used earlier in the project, where 2.1a and 5.2 were dispatched together under an explicit do-not-run-suites-concurrently caveat
- (decision) Sequencing after 2.1a lands, revised under the new no-caveat rule. The three-story parallel set this session first proposed is withdrawn: 3-1, 2-2, 2-1b and 2-6 are all store-backed, so every pairing carries the one-suite-at-a-time caveat and none qualifies as caveat-free. Run them in this order instead. First 3-1 corpus search, the longest pole on the demo path and the only candidate that needs nothing from 2.1a, so it can start before the 2.1a review even lands. Then 2-2 moment view, whose acceptance criteria were written to stand alone with an explicit empty right rail so it does not wait on Epic 4 extraction. Then 2-1b, which closes the eval gap by minting drops from the two NDA demo recordings that replace story 5.2's placeholder source ids. Then 2-6 as a small isolated defect fix
- (constraint) Story 5.3 is blocked on Epic 4, which the backlog order does not show. Its check 2.11 requires the harness to assert a scripted artifact is in neither retrieval store, then approve it VIA THE PUBLIC API, then assert it is in both with citations resolving to its source moment. That approval endpoint is story 4.3 per-moment approval and publishing, so 5.3 cannot be written against a publish gate that does not exist. Read alongside the SPEC sequencing constraint that the eval harness and all its setup complete before demo-script work begins, this means the eval track cannot be finished on the current epic order without 4.3, and the part of the eval track that CAN be closed early is story 5.2 going green, which needs 2-1b and the NDA recordings
- (decision) Story 2-7 parallel-safe store-backed tests filed, and the fix is tiered because the three stores differ. Postgres is easy and carries most of the win: three fixed database names, TEST_DATABASE in conftest plus meetingminer_test_pending and meetingminer_test_cli in test_migrations, all inside fixtures that already drop and create, with stores.postgres.database already configurable, so a per-run suffix and a teardown do it. Meilisearch is moderate: index names are code constants and config exposes only a url, so it needs a stores.meilisearch.index_prefix key the tests set per run. Neo4j is the hard one and cannot be solved the same way, because config carries uri and user but no database and Community edition supports exactly one, so there is no per-run database to create; the realistic options are a file lock around the four test files that touch it or a second test container, and a lock is preferred because it keeps that set explicitly serialized rather than pretending to be safe. Not started now because conftest.py is exactly what a story touching server will also edit and 2-1a is in review under server
- (note) Story 2.1a shipped a broken make target and 917 passing tests did not catch it. backfill-drop-paths interpolated the shared ARGS variable, which infra/Makefile defines globally as ARGS ?= --all for the rebuild target, while the backfill CLI accepts only --dry-run. So make backfill-drop-paths ARGS=--dry-run worked and the bare documented form failed every time with unrecognized arguments --all: the default path was the broken one, which is why nothing tested it. Fixed on main by giving the target its own BACKFILL_ARGS ?= variable, matching the EVAL_ARGS precedent already in the file
- (event) Story 2.1a is applied to the running dev environment, verified 2026-08-19. Migration 0008 applied, backfill converted 29 jobs, 51 transcript paths and 8 recording rows with 0 rows unplaceable under the drops root, and re-queued the 29 jobs the restarted worker had failed. Final state: every evidence stage done or skipped, only extract queued which is the expected steady state since extract has no implementation, 0 jobs with a null drop_relative_path, participants unchanged at 47 mail-keyed and 3 name-keyed so the morning augmentation survived the re-run, and a range request against the recording route returns 206 with the recording now resolved through the anchored path rather than an absolute one. Ordering lesson for the runbook: the backfill must run BEFORE the api and worker are restarted onto 2.1a code. Restarting first made the worker fail all 29 jobs for a missing drop_relative_path; recoverable because the backfill re-queues them, but it turned a clean migration into 29 failures
- (event) Story 2.7 built, pushed and handed to review on branch story/2-7-parallel-safe-store-backed-tests, range 9452382 to 141dc39, four files and 153 insertions. Postgres test databases are now per-run and self-dropping, the projection stores take a cross-process file lock because AD-4 fixes the Meilisearch index names and Neo4j Community serves one database, make test-db-prune sweeps what a killed run leaks, and AGENTS.md now says the server suite is concurrency-safe with make evals-run the one remaining serial case. Verified 917 tests unchanged plus two concurrent runs at 88 passed each. Built WITHOUT a spec file, unlike every other story here; the handoff says so and names the deferred-work entry and the sprint-status dispatch rule as the contract to judge against
- (decision) sprint-status.yaml conflict fix landed in two halves. First a key-wise git merge driver at _bmad/scripts/merge_sprint_status.py selected by .gitattributes and registered by install_merge_drivers.sh, now called from make bootstrap so a fresh clone cannot miss it. Reproduced the problem first: two branches changing ADJACENT story lines conflict with markers while two branches changing lines a few apart merge fine, which is why the conflicts felt random. Second, all narrative moved out of the file into a sibling sprint-notes.md, because the driver takes comments from ours and would silently drop a note added on the other branch. The file went from 156 lines to 94 with all 46 entries intact, and the driver was re-verified against the thinned real file
- (constraint) Fixing the test harness exposed the next chokepoint of the same shape: server/meetingminer/api/main.py. Every API story appends an app.include_router line and those lines are adjacent at 107 to 118, so two API stories conflict there for proximity rather than disagreement, exactly as sprint-status.yaml did. web/src/App.tsx is the same shape on the front end because it has no router at all, so any story adding a page edits it. Measured against the user's caveat-free rule this means only 2-1b, which touches pull_transcript and docs alone, is independent of everything; 3-1, 2-2 and 2-6 all touch api/main.py. A caveat-free trio does not exist until router registration stops being a hand-edited shared file
- (decision) Kickoff-instruction conflict resolved 2026-08-20: builders flagged that story contracts said hold the shared stores before store-backed suites while AGENTS.md post-2.7 says server suites run concurrently. AGENTS.md is authoritative - the contract lines predate 2.7 and cite AGENTS.md as their authority. Stale lines replaced in the 2.1a, 2.1b and 5.2 contracts on main (commit e794365), both running builders messaged; only make evals-run stays serial. No kernel change - operational instruction, not contract
- (decision) Sequencing constraint re-rendered to carry the hidden 5.3-on-4.3 dependency already logged as a constraint: check 2.11 approves an artifact through the public per-moment approval endpoint (CAP-6, story 4.3), so the eval harness cannot finish before that endpoint exists; the capture checks (5.2) are the early-closable part, needing only the NDA demo drops (2-1b). Kernel edit confined to the one Constraints line; capabilities, non-goals, success signal unchanged
- (event) Spec update self-validate 2026-08-20: pass 1 coherence PASS - single Constraints line amended, rules 1-6 and 8 hold, all CAP ids stable and untouched. Pass 2 preservation PASS - the 5.3-on-4.3 constraint entry now renders in the kernel; remaining recent memlog entries (no-caveat dispatch rule, story order, router chokepoint, 2.7 harness fix, schema-cache defect) are process or implementation state with homes builders already read (sprint-notes.md, deferred-work.md, AGENTS.md), not contract claims
- (decision) Story 5.5 (branch story/5-5, merging soon) added an operationalization note to companion eval-design.md pointing at evals/RUNBOOK.md and the four full designs under evals/designs/ (citation-timestamp-window, action-item-fuzzy-match, retrieval-eval, eval-cadence); the section text itself is unchanged and stays the contract of record. Logged here because eval-design.md is spec-authored and bmad-spec is its single writer: any future re-derive must preserve this note or regenerate its equivalent, not silently drop it. The hand-edit is accepted retroactively rather than reverted
- (decision) User decision 2026-08-20: extraction granularity is the WHOLE TRANSCRIPT, not the moment. Story 4.1's per-moment extraction is wrong - a decision emerges across minutes (proposal, pushback, agreement) and almost never sits inside one moment; 358 paid claude-sonnet-5 calls over 5 meetings proved the shape before the user stopped it. The proven mechanism already exists in pull_transcript generateDocs: entire timestamped transcript to Ollama gpt-oss:120b (10.77.0.52, num_ctx 65536, bake-off winner 7/16/26) through arch_summary_prompt.md and action_items_prompt.md, every extracted item anchored with m:ss timestamps and grounding rules (no invented dates, Proposed tags, Confirmed/Assumed/Open marks). MeetingMiner's extract stage must adopt this: one whole-transcript pass per meeting through the Llm port, default model ollama gpt-oss:120b (free, local), prompts adapted from the proven pair, and each artifact's timestamp anchor resolved deterministically to its containing moment for the FK link - which preserves no-citation-no-answer. CAP-5 re-render required: extracts from the meeting transcript, artifacts anchor to moments
- (constraint) Extraction default model is local (ollama gpt-oss:120b per the pull_transcript bake-off), not a paid API. Paid-model extraction requires fresh per-run authorization - the user revoked the Anthropic key after stopping the 4.1 backfill at 5 of 28 meetings
- (event) Spec update self-validate 2026-08-20 (extraction granularity): pass 1 coherence PASS - CAP-5 intent re-rendered to whole-transcript extraction with timestamp-to-moment anchoring, success signal unchanged, new local-model-default constraint added, all CAP ids stable. Pass 2 preservation PASS - the user decision, the pull_transcript mechanism facts (model, host, num_ctx, prompt pair, bake-off), and the paid-authorization rule all landed in SPEC.md, sprint-notes (story 4-1a), or the memlog; the divergent-working-copies observation recorded in sprint-notes
- (decision) User resolved the 4-1a design choice 2026-08-20: adopt-when-present, generate-when-absent. When a new transcript arrives with extraction docs already produced by another source (the puller summariser's arch-summary and action-items markdown), the extract stage PARSES those - no model call. Only a transcript arriving without extractions is sent to Ollama by MeetingMiner itself, through the Llm port with the adapted prompts. Both paths converge on one strict parser (both known summariser layouts) producing timestamp-anchored artifacts resolved to moments. Derivative documents are created only when necessary, never regenerated. Consequence for the drop contract: the puller's emit-drop should carry the summary docs when they exist (optional files, schema addition) so adopted extractions arrive as evidence with provenance like everything else - arrived material, drops root, per-file row with sha256
- (decision) Spec re-derived 2026-08-20 to render the adopt-when-present decision the memlog carried but SPEC.md did not (memlog was one entry ahead of the last derive, commit 10d2619 vs 79f19c0). Kernel edits confined to CAP-5 intent - derivative documents created only when necessary, so a drop arriving with the summariser's extraction documents is parsed with no model call and only a transcript arriving without them is sent to a model, both paths converging on one strict parser that reads both known summariser layouts, plus the anchor that resolves to no moment being a named error rather than a dropped artifact - and to the drop-contract assumption, which now states that emit-drop carries those documents as optional schema files so an adopted extraction arrives as arrived material with a per-file row and sha256 under the drops root rather than through a side channel. Companion storage-layout.md section 2 carries the same addition in the drop anatomy, with the at-least-one-evidence-file rule tightened so the derivative documents cannot satisfy it alone. Canonical filenames deliberately not pinned here: they are the schema addition story 4-1a makes
- (event) Spec update self-validate 2026-08-20 (extraction sourcing): pass 1 coherence PASS - CAP-5 keeps both intent and success, ids CAP-1 through CAP-9 stable and untouched, constraints, non-goals and success signal unchanged, no new assumption or open question since the unpinned canonical filenames are a schema decision story 4-1a owns rather than a spec gap. Pass 2 preservation PASS - every claim in the pending memlog entry landed: adopt-when-present and generate-when-absent, never regenerated, the single strict parser over both known summariser layouts, and the timestamp-to-moment anchor in CAP-5; the emit-drop optional-files schema addition with drops-root provenance in the Assumptions drop contract and in storage-layout.md section 2. The paid-model authorization rule it restates was already a constraint and needed no edit
- (event) Inspection 2026-08-29 after the output folder was deleted, restored and identifier-redacted: SPEC.md and all 10 listed companions present and parse; no redaction placeholders inside this folder; all companions: paths resolve. No derive this session — inspection only
- (note) Broken source path: SPEC.md sources[0] and memlog entry 6 name the capstone brief with a pseudonym-substituted owner surname; the committed file is tim.goeke-capstone-product-brief.md (the 'Blake' token is the identity map's owner-surname replacement). Fix on next derive; the two brainstorm-intent sources resolve
- (note) eval-design.md section 1 example is split-pseudonymised: presenter rows carry the pseudonym but the QA question still names the owner by real first name, and the committed manifests evals/ground-truth/demo-001-*.yaml carry the owner's real full name. One convention is needed (companion follows committed ground truth, or both pseudonymise) before the next derive
- (note) Events since the last entry (2026-08-20) that the kernel and companions do not reflect: real corpus purged 2026-08-22 (only the two scripted demos remain; new corpus to be recorded in an owned Teams sandbox); puller moved pull_transcript/ to tools/puller/ 2026-08-22; docs/architecture.md (17 ADs) became the committed technical contract superseding ARCHITECTURE-SPINE.md; demo drops rewritten to their fictional source ids and all stores rebuilt 2026-08-28. Stale in SPEC.md: the corp-production-tenant constraint, the two-NDA-demo-recordings legacy-parser constraint, the ~120-minute/117.6-min sizing premise, the personal-OneDrive recovery premise, the pull_transcript puller assumption and CAP-5 prompt provenance, and 'AD-1-16' in Open Questions (backlog B-31). Stale in companions: scope.md corpus bullet, ux-spine.md L7, architecture-diagrams.md L7; corpus-facts sections 1-4 and 6, capture-measurements sections 1-5 and retrieval-prior-art sections 6-8 are measurements of the purged corpus cited as live
- (note) Companion drift against shipped code: storage-layout.md L111-113 describes pre-2.1a state as current (migration 0008 shipped); capture-measurements.md L97 says crop auto-detection is unimplemented (story 1-11 shipped survey-based detect_share_region); glossary.md L71-72 omits the schema-v3 extraction-document filenames docs/source-drop.schema.json pins; eval-design.md check 2.2 budget formula lacks the shipped extra term (backlog B-32); ux-spine.md meeting-level composition is superseded by spec-ui-reimagine CAP-1/2 without saying so
- (note) Identifier classes for owner review, none present in the tracked tree: a third-party integration-product name (scope.md L20; 9 output-tree files), an internal team acronym in an inventory filename (corpus-facts.md L234), a private LAN address of the ASR host (corpus-facts.md L205). Recorded in class terms only
- (direction) Owner 2026-08-29: the cohort close-out needs the app running and a recorded ~5-minute walkthrough; no deadline ('all the time in the world'). Corpus source changes: published YouTube videos (conference talks with screen shares, recorded community meetings) acquired by pasting a URL, plus past Teams recordings with transcripts, plus Zoom calls with transcripts — 'leave all our options open'. Supersedes the owned-Teams-sandbox corpus premise
- (direction) Owner 2026-08-29: 'I want an amazing UI experience. Do not get lazy' — the acquisition flow is a first-class UI feature, not a CLI with a UI afterthought; UI work is not to be scoped down to the minimal path
- (direction) Owner 2026-08-29: both the OpenAI and Anthropic keys are topped up and working; either provider may be used as the agent sees fit, and the model must be a configurable option in the user interface. Supersedes the 2026-08-21 key-revoked premise and the 'UI never mutates bindings' constraint from spec-ui-reimagine/spec-system-status — an AD-10 amendment is owed (config declares the catalog and defaults; a persisted user selection picks among them). No-silent-fallback still stands
- (direction) Owner 2026-08-29: for YouTube content, speaker attribution is assigned after the fact — the system identifies who-spoke-when (diarization into anonymous turns), then a human assigns names to the turns. Consistent with never-guess (machine segments, human names), AD-5 (assignment is api-owned curation via participant_alias), AD-13 (attribution attaches to cue timing, so moment ids and citations survive re-attribution)
- (event) Sprint Change Proposal 2026-08-29 (revision 2) APPROVED by the owner: Epics 6 (Bring Any Meeting In), 7 (Know Who Spoke), 8 (Choose the Model, AD-10 amendment), 9 (Cohort Close-out) added to epics.md with FR33-FR39 and UX-DR12-15; tracking regenerated; kickoff of 6.1 (UX design spec) and 6.2 (YouTube acquisition command). Contract of record for these requirements is epics.md plus the committed docs/ tree; SPEC.md not re-derived
- (direction) Owner 2026-08-29: 'the UI needs to be amazing.' Two primary views from earlier prototypes: MOMENTS — the most interesting or pressing items presented first; THREADS — a topic followed across meetings on a timeline. Added as Epic 10 (FR40-43, UX-DR16-18); Topic nodes do not exist in the graph today (participant-topic traversal is a text CONTAINS over Moment.text), so topic extraction and Thread linking are new evidence-derived navigation, outside the publish gate (AD-4 clarification owed with story 10.2)
- (direction) Owner 2026-08-29: Threads start zoomed out and zoom in like Google Earth — details revealed per level (thread bands → meetings → moments → evidence → replay), each level fetching only what it renders, nothing invented at any level (FR43, UX-DR18; stories 10.3 and 10.6)
- (direction) Owner 2026-08-29: 'tests cause conflicts between builders and tests take forever to run. As the operator I want the next thing built to be a much better and more efficient test suite.' Added as Epic 11 (NFR19 seconds-fast default suite, NFR20 per-run store isolation incl. evals-run; closes B-1, B-4, B-14) and placed FIRST in the build order: 11 → 6 → 10 → 7 → 8 → 9. 6.2 waits for 11.1/11.2 to land because both touch server/tests
- (direction) Owner 2026-08-29: 'If we need to stand up multiple stacks — this laptop has 128 GB of RAM — let's make the most of it.' Story 11.2 amended: make worktree can provision a full private compose stack (Postgres, Neo4j, Meilisearch) per worktree on dynamic ports; AGENTS.md's stores-are-shared section to be rewritten when it lands
- (direction) Owner 2026-08-29: 'when we create the amazing UI let's make sure there's a bit of color' — 6.1's design brief now requires a color system on the dark data-dense base where color carries meaning (thread identity, moment kind, ingestion state, provider health), accessible, not decorative
