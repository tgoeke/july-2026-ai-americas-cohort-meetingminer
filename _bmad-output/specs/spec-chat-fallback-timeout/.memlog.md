---
topic: Chat turn dies at the web client's 60s timeout while the api completes the answer at ~102s on the local fallback model, after the invalid Anthropic key silently disables the primary
updated: 2026-08-29T13:31
---

- (note) Input was the raw browser error 'Cannot reach the api at http://localhost:8000: timed out after 60000ms'; message text traced to web chat panel (CHAT_TIMEOUT_MS=60_000 is the only 60000ms bound in web/src)
- (note) Evidence from .logs/api.log 2026-08-21T20:33-20:35Z: primary claude-sonnet-5 fails in ~1.6s with AuthenticationError 'API key is invalid', fallback ollama/qwen3:30b engaged; classification ~19s, synthesis ~80s, chat.completed elapsed_ms=102007; api was up (health 200) the whole time
- (decision) Break-fix scope is the degraded-path behavior, not the key: fallback is a supported configuration and must survive on its own; key rotation is an ops action logged as an open question
- (capability) CAP-1: a chat turn answered by the fallback model completes in the UI instead of dying at the client timeout; success = with the primary unavailable the same question returns a cited answer, no transport failure
- (capability) CAP-2: fallback engagement and the primary's auth failure are visible outside the server log; success = an invalid-key turn shows which model answered and that the primary is failing
- (capability) CAP-3: a client-side timeout on an accepted request is reported as a timeout, not as 'Cannot reach the api'; success = the unreachable-api wording appears only when no connection was established
- (constraint) Story 3.3 invariant holds: the whole answer is validated before any chat.token streams; no fix may stream unvalidated tokens to beat the timeout
- (constraint) No paid verification: reproduce and verify on the free path (invalid/absent key forces fallback); a paid Anthropic call needs a fresh explicit yes
- (constraint) Worker restart hold unchanged (~850 queued paid extraction calls); nothing in this fix restarts the worker
- (assumption) 102s was measured for one short question at 28 retrieved moments on qwen3:30b; fallback latency scales with prompt size, so 60s is not merely marginally short
- (question) Rotate the Anthropic key? Ops action for the user; note the same invalid key will fail the ~850 queued extraction calls whenever the worker restarts
- (question) Earlier same-day failures at 15:02Z were fast 503s from the then-dead fallback tag qwen3:32b (fixed to :30b in config 2026-08-20) — confirm no other role still rides a dead tag
- (event) Self-validate pass 1 (coherence): pass — CAP-1..3 each intent+success, intents are WHAT-level, constraints bend decisions, non-goals present, success signal testable on the free path
- (event) Self-validate pass 2 (preservation): pass — the input error line is verbatim in Why; every api.log claim used is in failure-evidence.md; no wrapper-only drops
- (note) Provenance answered: the fallback mechanism is specified in ARCHITECTURE-SPINE.md AD-10 (line 229, architecture-meetingminer-2026-08-16): 'Default bindings: extraction + chat = claude-sonnet-5 (cloud primary), Ollama models as configured fallback' — implemented in story 4-1 (commit 621af90, build_llm call-time fallback composer)
- (direction) Owner 2026-08-21: the Anthropic key was invalidated deliberately (unauthorized paid use); do not rotate it. Paid roles move to the OpenAI key. Owner never agreed to the silent fallback mechanism and wants it cleaned up
- (decision) config.yaml: chat and judge -> openai/gpt-5.2, fallback keys removed; openai/ prefix required because the adapter's bare-name routing (_BARE_OPENAI_PREFIXES) predates gpt-5. Api restarted (config loads once at startup); health 200
- (decision) CAP-1 and CAP-2 retired with the fallback: superseded by CAP-4 — a failed primary surfaces as a prompt visible error, never a silent substitute and never a 60s hang. CAP-3 (timeout wording) stands
- (question) Extraction role still carries ollama/gpt-oss:120b -> ollama/qwen3:30b fallback (local-to-local); owner decision pending on whether the no-silent-fallback rule removes it too (changes worker behavior; worker restart hold unchanged)
- (note) Where this configuration is documented: config.yaml itself (commented, AD-8/AD-10 authority), ARCHITECTURE-SPINE.md AD-10 (now stale on default bindings — spine amendment owed via bmad-architecture), project-context.md, .env.example for secret names
- (event) Self-validate after update: pass — CAP-1/2 retired with IDs preserved, CAP-3/4 carry intent+success, owner directions preserved verbatim in Why/Constraints, spine-staleness and extraction-fallback questions on record
- (decision) Owner 2026-08-21: extraction's local-to-local fallback (ollama/gpt-oss:120b -> ollama/qwen3:30b) stays — acknowledged and accepted; the no-silent-fallback rule applies to paid/cloud substitution on the chat path, and this one costs nothing and was surfaced. Open question resolved
- (event) Dispatched to builder: build-prompt-story-chat-fallback-timeout-2026-08-21.md, branch story/chat-fallback-timeout, own worktree; dispatcher lands the branch after review
- (event) Landed on main at 8080a1a after dispatcher review and rebase; web-test 203/203, test_api_chat.py 47/47 on the rebased range; api restarted onto merged code. Remaining: owner-triggered live OpenAI turn, spine AD-10 amendment
- (event) Open question on the spine AD-10 default-bindings sentence CLOSED by supersession, verified 2026-08-29: docs/architecture.md is the committed technical contract and its AD-10 carries no default-bindings sentence; config.yaml binds chat and judge to openai/gpt-5.2 with no fallback. The stale sentence survives only in the untracked planning spine (ARCHITECTURE-SPINE.md:229), which the committed docs declare superseded
- (note) The constraint 'worker restart hold unchanged' rests on an expired premise: extraction is bound to a local model and the paid queue is empty (docs/backlog.md B-30). Retire on next derive; the no-silent-substitution rule stands
- (decision) 2026-08-29 re-derive: constraint 'worker restart hold unchanged' retired - its premise expired (extraction bound to a local model, paid queue empty, docs/backlog.md B-30); the no-silent-substitution rule stands
- (direction) Owner 2026-08-29 (spec-meetingminer memlog): both Anthropic and OpenAI keys are valid, either provider may be used, and the model becomes a UI-selectable option (Epic 8, AD-10 amendment). Supersedes the 2026-08-21 'Anthropic key invalid by decision' premise: the non-goal 'rotating or restoring the Anthropic key' is retired; CAP-3, CAP-4, the openai/gpt-5.2 binding and no-silent-fallback stand; per-role catalog and persisted selection are Epic 8 scope, not this spec
- (note) Assumption 'first real chat turn is the test' met: spec-ui-reimagine memlog records the chat role provider account topped up and ask-the-corpus verified live 2026-08-22 (break-fix 884404f). Rendered as resolved
- (note) Open question on the spine AD-10 sentence rendered as resolved (closed by supersession 2026-08-29, see prior event); the planning spine is untracked and superseded by docs/architecture.md
- (event) Re-derived 2026-08-29. Self-validate pass 1 (coherence): PASS - CAP-1/2 retired with ids kept, CAP-3/4 carry intent+success at WHAT level, every constraint rules something out, three non-goals, success signal testable on the free path
- (event) Self-validate pass 2 (preservation): PASS - the 2026-08-29 key-restore direction, the B-30 retirement, the spine closure and the live-turn note each land in Why, Constraints, Non-goals or Resolved; Epic 8 scope is pointed at epics.md, not duplicated; no wrapper-only drops
