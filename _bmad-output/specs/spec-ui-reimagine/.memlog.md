---
topic: Reimagined corpus-revealing UI + config exposure, demo-scoped
updated: 2026-08-29T11:00
---

- (direction) User verdict: UI is anemic — a bare video list that reveals nothing of the corpus; reimagine it
- (direction) User: no configuration is exposed in the UI; wants config surfaced
- (constraint) Demo is tomorrow morning (~12h, 2026-08-22 AM): scope to what one overnight build can land; demo-rescue, not redesign
- (direction) User supplied competitor reference screenshot (reference-competitor-meeting-view.png): dense 3-column meeting view — header stats (date/duration/turns/words/passages/source lineage), screens film-strip with timestamps, timestamped speaker transcript passages, right rail of extracted actions (timestamp+owner+status), risks, participants, topics w/ frequencies, generated documents w/ sizes
- (note) Config survey: config.yaml 480 lines (llm roles+prompts, capture thresholds, search knobs, stores) file-only, strict fail-fast load once at startup, no hot reload; API exposes only getExtractionPrompts + config_version in health; no config mutation endpoint; .env holds secrets + 3 roots
- (note) Existing UI stories done: ingestion progress, moment view, meeting drill-down, participant curation, corpus search, chat w/ streaming cited answers, approval publishing, prompt viewer — capability exists, front door hides it
- (note) UI survey: only 3 child routes (meeting drilldown, moment view, participants); home = search+chat+bare meeting rows; meeting row shows title/date/status only — no poster, counts, duration, filtering; unused API surface incl listMeetingMoments, search filters (meetingId/corpus/limit/offset), SearchHit metadata, speakerResolution, artifact cross-corpus views; chat is one-shot; no dashboard, no global nav
- (decision) Express mode: distill without elicitation; demo deadline forbids guided walkthrough (user memory: decide and execute)
- (decision) CAP-1 Corpus-revealing home: dashboard stats header + meeting evidence cards (poster screenshot, duration, moment/screen/artifact/participant counts, corpus filter/sort); search+ask promoted to persistent chrome
- (decision) CAP-2 Dense meeting evidence view modeled on reference screenshot: header stat line + screens film-strip + timestamped speaker transcript + right rail (artifacts by kind w/ timestamp anchors+state, participants incl explicit absence, published docs)
- (decision) CAP-3 Config transparency page: read-only, one new GET endpoint serving an allowlist-sanitized projection of Settings (models/prompts/thresholds/knobs/stores); page states the change path is config.yaml + restart (AD-8/AD-10); extends main-spec CAP-5 prompt-visibility mandate
- (decision) CAP-4 Demo-path continuity: search → cited answer → open moment → replay runs through the new chrome; dry-run tonight is the gate
- (constraint) New backend strictly read-only (stats, roll-ups, sanitized config); no config mutation from UI; secrets/.env values never serialize
- (constraint) Render only data that exists: no topic-frequency chips or risks sections invented for looks; reference elements without backing data are post-demo
- (constraint) Build order CAP-1 then CAP-2 then CAP-3 then CAP-4 gate; each independently landable; unfinished pieces fall back to existing screens for the demo
- (decision) Companions: current-ui-inventory.md (spec-authored, survey findings), reference-ui.md (spec-authored, screenshot + element-to-data mapping); adopted: ../spec-meetingminer/SPEC.md (inherited constraints). ux-spine.md moment-view anatomy stays valid; this spec supersedes only the meeting-level composition
- (event) Self-validate pass 1 (coherence): PASS — CAP-1..4 all carry intent+success, constraints bend decisions, non-goals explicit, success signal demonstrable, IDs unique
- (event) Self-validate pass 2 (preservation): PASS — rant complaints, 12h deadline, and reference screenshot all land in kernel or companions; survey findings preserved in current-ui-inventory.md; reference elements without backing data logged as non-goals/open questions, not dropped silently
- (decision) Story breakdown: 5 stories — 1 read-only reveal API (stats + per-meeting roll-ups + sanitized config endpoint, single client regen), 2 corpus-revealing home (CAP-1), 3 dense meeting evidence view (CAP-2), 4 config transparency page (CAP-3), 5 demo dry-run gate (CAP-4)
- (decision) All new endpoints and the one 'make client' regen live in story 1 so generated web/src/client is written by exactly one story; stories 2-4 are UI-only and parallelizable after 1
- (decision) User: full-auto overnight — no spec_checkpoints; done_checkpoint only on story 5 so dispatch pauses for morning review
- (decision) User authorized ~3-5 paid /chat calls (openai gpt-5.2 role) solely for story 5's demo dry-run; nothing else paid runs (fresh explicit yes 2026-08-21 evening)
- (event) stories.yaml schema check: PASS — 5 entries, required fields present, ids quoted/unique/prefix-free, no status field, checkpoints per user (done_checkpoint only on 5), invoke_dev_with carries dispatch notes only
- (note) Dispatch-time discovery: freeform story system-status landed 2026-08-21 (merge 3f5a1ea) — GET /status + chrome indicator + /status page already expose llm.roles bindings via test-pinned allowlist. Story 1/4 extend that allowlist pattern (registry gained 'status' route); story 2 chrome keeps the status indicator; App.test.tsx fetch mock is URL-aware
- (event) ui-5 demo dry-run: UI path fully demo-ready (all 4 stories landed, 250 web tests green); TWO demo-blocking infra issues found outside this spec's scope — Meilisearch invalid_search_embedder (0 hits), OpenAI gpt-5.2 out of credits. done_checkpoint reached, chain paused for owner
- (event) Both ui-5 demo blockers cleared 2026-08-22 (sprint-notes.md, ask-the-corpus break-fix section): the zero-hit search was the Docker runtime being off, not the index; the chat role's provider account was topped up; a separate artifact-lane matching-strategy defect was fixed at 884404f. Spec chain complete; only the post-demo topic-chips question remains open
- (note) reference-ui.md cites reference-competitor-meeting-view.png 'in this folder'; the file is absent on disk and in every copy on this machine (deleted before the restore per spec-puller-source-relocation.md:34-37). Either restore it from the archive backup or reword the companion to stand without the image
