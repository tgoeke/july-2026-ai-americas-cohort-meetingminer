# Review — Story 1-8 (2026-08-18) — LOST, loss record

**This is not the review report.** The review dispatched by
`review-prompt-story-1-8-2026-08-18.md` was completed in the reviewing session's
terminal and never written to disk; the session is gone and the report's
prose, severities, and evidence are unrecoverable. This stub exists so the
loss is on the record and `make check-reviews` distinguishes recorded losses
from silent ones.

**Surviving evidence:** no [Review] entries found in the story spec — the review's findings, if any, survive only in the story's post-review fix commits (`spec-1-8-teams-puller-emits-source-drops.md`), plus any `fix(...)` commits in the
story's branch range.

Recorded 2026-08-20, when `make check-reviews` was introduced after the
fourth unfiled review (story 3.1, recovered because its session could be
resumed).
