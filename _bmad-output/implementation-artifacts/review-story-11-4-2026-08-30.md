# Code Review — Story 11-4: Lint and Type Tooling in the Fast Loop

## Scope

- Review worktree: `/Users/devopsterus/current/cohort/meetingminer-wt/11-4-review`
- Reviewed branch: `story/11-4`
- Report branch: `story/11-4-review`
- Review range: `origin/main..origin/story/11-4`
- Spec: `_bmad-output/implementation-artifacts/spec-11-4-lint-and-type-tooling-in-the-fast-loop.md`
- Review mode: findings only; no implementation fixes

## Findings

### Finding 1 — The required cross-lane conflict check is no longer clean

**Location:** `_bmad-output/implementation-artifacts/sprint-notes.md:2732`

**Severity:** medium

**Finding:** The story records and requires `branch_conflicts.py` to be clean apart from the excepted `story/11-2-review` pairs and the named `server/uv.lock` overlap with story 7-1, but the required command now reports additional `sprint-notes.md` conflicts with stories 10-1, 11-3, and 7-1. This leaves a frozen `Block If` condition and a required verification command unmet; the branch cannot be treated as integration-ready from the recorded evidence.

**Evidence:** From the review worktree, `python3 _bmad/scripts/branch_conflicts.py --against story/11-4` exited non-zero and printed, among the expected pairs:

```text
story/11-4 × story/10-1          CONFLICT: _bmad-output/implementation-artifacts/sprint-notes.md
story/11-4 × story/11-3          CONFLICT: _bmad-output/implementation-artifacts/sprint-notes.md
story/11-4 × story/11-3-review   CONFLICT: _bmad-output/implementation-artifacts/sprint-notes.md
story/11-4 × story/7-1           CONFLICT: _bmad-output/implementation-artifacts/sprint-notes.md, server/uv.lock
story/11-4 × story/7-1-review    CONFLICT: _bmad-output/implementation-artifacts/sprint-notes.md, server/uv.lock

13 clean pair(s), 10 conflicting pair(s).
```

The story's own note at lines 2732–2735 instead says every pair is clean except the `story/11-2-review` exceptions and the `server/uv.lock` pair.

**Suggested direction:** Reconcile the append-at-EOF `sprint-notes.md` overlaps during integration (or rebase/remediate the story once the active lanes land), update the recorded conflict state, and rerun the exact conflict command until only explicitly accepted pairs remain. Do not change the lint/type implementation to address this process-file collision.

### Finding 2 — The baseline is described as 49 entries although it has 38 entries and 49 pairs

**Location:** `server/pyproject.toml:153`

**Severity:** low

**Finding:** The changed pyproject comment says the measurement leaves “49 per-file entries,” and the new sprint note repeats “49 per-file baseline entries.” The table actually contains 38 path entries holding 49 file/code pairs. Elsewhere the story correctly distinguishes those units, so the two inaccurate statements make the retirement unit and expected table size ambiguous for a builder shrinking the baseline.

**Evidence:** `uv run --project server python` with `tomllib` over the committed configuration printed:

```text
per-file entries=38 pairs=49
```

The conflicting changed lines are:

```text
server/pyproject.toml:153:# codes carried 151 of them, leaving the 49 per-file entries below.
_bmad-output/implementation-artifacts/sprint-notes.md:2722:49 per-file baseline entries; mypy over the 13 decision-core modules with
```

The merge-base recount independently produced `remaining_pairs=49 files=38` from ruff 0.16.5 at `211857c`, after excluding the six isolated-run global codes.

**Suggested direction:** Describe the table consistently as “49 file/code pairs across 38 per-file entries” in both changed locations; keep the configuration and its counts unchanged.
