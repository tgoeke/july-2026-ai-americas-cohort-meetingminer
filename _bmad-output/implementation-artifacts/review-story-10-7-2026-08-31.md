---
story: 10.7
story_key: 10-7
date: 2026-08-31
reviewer: Codex
branch: story/10-7-review
source_branch: story/10-7
status: complete
verdict: pass
---

# Adversarial review — Story 10.7: Threads Is a Query, Not a Catalogue

## Review scope

- Source branch: `story/10-7`
- Review/remediation branch: `story/10-7-review`
- Frozen specification: `_bmad-output/implementation-artifacts/spec-10-7-threads-is-a-query-not-a-catalogue.md`
- Prototype authority: `/Volumes/nvmepool/mm_current/meetingminer/api/thread.js`, `web/src/Thread.tsx`, and `web/src/ZoomTimeline.tsx`
- Original builder range: `8bd54e868c591f000417ef916476500e768c7c18..82676eab9b32819f7d81103dd4e832b1e4f07701`
- Rebased verification range: `a1d9182e..f2fff78e`
- Review state: complete; all three patchable findings were remediated red-first on the review branch.

## Findings

### F1 — High — Synonymous topic rows make a complete trace claim it was capped

- Source: `acceptance-auditor+edge-case-hunter`
- Location: `server/meetingminer/api/threads.py:280`, `server/meetingminer/api/threads.py:302`, `server/meetingminer/api/threads.py:1059`
- Triage: `patch`
- State: remediated red-first

`_TRACE_STOPS` reports `COUNT(*)` topic-mention rows while `_TRACE_MOMENTS`
quotes distinct `moment` rows selected through `IN`. A derived thread can contain
two synonymous topics attached to the same moment, so one quoted moment is
counted twice in `mentionTotal`. `_assemble_trace` then compares the distinct
quoted rows to that larger total and prints a capped `N of M moments` sentence
even when every distinct moment is present. This violates the story's central
honesty rule and the explicit claim that the two cap figures describe the same
row set. The fix must count subject occurrences on the same distinct-moment row
set the quoting query uses, while retaining every meeting stop.

Red evidence: `test_synonymous_topics_on_one_moment_do_not_fake_a_cap`
failed with `mentionCount [2, 1]` against the unfixed query. The stop aggregate
now counts distinct live moment ids, matching the row set `_TRACE_MOMENTS`
quotes.

### F2 — High — Navigating from a thread deep link to `/threads` keeps the old trace visible

- Source: `acceptance-auditor+edge-case-hunter`
- Location: `web/src/features/threads/ThreadTrace.tsx:76`
- Triage: `patch`
- State: remediated red-first

The route effect loads when `threadId` is present but does nothing when it
becomes absent. React Router can reuse the same `ThreadTrace` component across
the sibling `/threads/:threadId` and `/threads` routes, preserving component
state. Following the primary Threads navigation from a deep link therefore
leaves the previous timeline on the supposedly empty front door; an in-flight
deep-link response can also land after the route became bare. This recreates
the exact interaction Story 10.7 exists to remove. The bare route must clear the
query/trace/refusal state and invalidate any older request generation.

Red evidence: `returns to the empty front door from a deep-linked subject`
could not find the empty-state heading after navigating to `/threads`; the old
trace and input remained visible. The route effect now clears the bare route
and advances the request generation before an older response can land.

### F3 — Medium — Initial fitting can use the 1000px fallback instead of the rendered width

- Source: `edge-case-hunter+verification-gap`
- Location: `web/src/features/threads/TraceTimeline.tsx:76`, `web/src/features/threads/TraceTimeline.tsx:110`, `web/src/features/threads/TraceTimeline.tsx:127`
- Triage: `patch`
- State: remediated red-first

The first passive fit can consume the state initializer's 1000px width and mark
the trace label fitted while the layout effect is publishing the element's real
width. The rerender with the measured width then refuses to fit again because
the label is already recorded. On a viewport narrower than 1000px, the final
meeting can start off-screen even though the view claims to open where the whole
span fits. Fitting ownership must include the measured width (and trace extent),
not only the label, without resetting a reader's zoom after the same measurement
has already been applied.

Red evidence: `fits the whole span to the rendered width, not the fallback
width` received `data-ppd="7.46"` at a 500px rendered width instead of `3.22`,
and the final stop was culled. The component now waits for a positive layout
measurement in browsers, uses 1000px only when no layout observer exists, and
keys first-fit ownership to the trace's identity and extent.

## Verified mechanisms

- **Semantic zoom is genuine.** A search over the whole threads feature and
  its mounted shell ancestors found no `transform: scale`, CSS `zoom`, or
  scale utility. The only transform is per-card `translateX` centering.
  Geometry is recomputed in pixels per day, altitude changes representation,
  and labels retain fixed pixel font sizes across the complete altitude range.
- **Lanes repack at the current altitude.** `TraceTimeline` derives lanes in a
  memo whose dependencies include `ppd` and `cardWidthPx`. The same two stops
  occupy two lanes at 8 px/day and one at 210 px/day; no assignment survives a
  zoom change.
- **Capping is per meeting, with no stop cap.** `_TRACE_MOMENTS` uses
  `ROW_NUMBER() OVER (PARTITION BY mo.meeting_id ...)`; `_TRACE_STOPS` has no
  `LIMIT`. A late meeting remains present when earlier meetings exceed the
  per-meeting cap. F1 repaired the only row-set mismatch: synonymous topic rows
  now count the distinct moments the quote query can return.
- **Suggestions are span-ranked inside an inclusive band.** Both meeting-count
  edges are eligible, an over-band subject is excluded, and a corpus too small
  to fill the band is returned as-is rather than silently widened. Calendar
  span precedes meeting count in the ordering, and near-duplicates are dropped;
  mention frequency is not a ranking input.
- **The honesty contract is explicit end to end.** Missing `mode` or
  `completenessNote` is a parser refusal. Exhaustive and sampled/capped results
  cannot share the same prose. Recorded meetings without quoted stills say no
  screen was captured at those moments, while transcript-only meetings say no
  recording exists. A stop carrying a screenshot renders it, and each meeting
  card provides an `/meetings/:meetingId` destination.

## Filed-gap assessment

- **B-59 remains open.** `infra/Makefile` proves `make client` requires a live
  MeetingMiner API health check and `/openapi.json`; this review did not start
  the API during the paid-model ingest. The raw-fetch boundary is justified.
- **B-60 is closed by this review.** Its implementation was sound, but the
  seeding-cost rationale was weak: the necessary screen chain is two compact
  inserts already used elsewhere. Positive server and browser tests now cover
  `screenCount`, opaque `screenshotId`, and rendered media URLs.
- **B-61 is closed as not applicable.** Migration 0002 makes
  `meeting.started_at` `NOT NULL`, so no undated stop can reach this trace.
  Review tests pin the reachable `day`-precision behavior from the SQL anchor
  through the browser's `date only` label.

## Verification log

- Red-first F1: `test_synonymous_topics_on_one_moment_do_not_fake_a_cap`
  failed with `[2, 1] != [1, 1]` before the SQL repair, then passed.
- Red-first F2: `returns to the empty front door from a deep-linked subject`
  failed while the old trace remained mounted, then passed after route-state
  invalidation.
- Red-first F3: `fits the whole span to the rendered width, not the fallback
  width` observed 7.46 px/day instead of 3.22 at 500px, then passed after fit
  ownership moved to the real measurement.
- `make lint`: all checks passed.
- `web/node_modules/.bin/tsc -b`: clean, exit 0.
- `oxlint src/features/threads`: no finding in Story 10.7 or its remediation;
  seven warnings remain only in the deliberately retained Story 10.6 files.
- Targeted server thread suites: **92 passed** in 2.82s.
- `make web-test`: **788 passed, 65 files**.
- `make test`: puller **128 passed**, web **788 passed**, eval harness **655
  passed**, isolated worker/STT/diarization group **92 passed**, store probe **1
  passed**, complete server suite **2901 passed, 3 skipped** in 12m16s, and the
  production web build passed; exit 0.
- `origin/main` was fetched after the full gate. The review branch remains
  rebased on current `origin/main` `a1d9182e`.
- The API and worker were not started, `make evals-run` was not invoked, and no
  paid model was called.

## Final verdict

**Pass after remediation.** The three implementation defects are fixed and
held by regression tests. B-59 remains a candid, separately filed client-code
generation gap that requires a safe live-API window; it does not make the
strict raw-fetch implementation dishonest. B-60 is closed with positive screen
coverage, and B-61 is closed as unreachable under the `NOT NULL` schema with
the reachable day-precision path pinned end to end. No owner decision or frozen
spec defect remains open.
